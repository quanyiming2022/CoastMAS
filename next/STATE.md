# CoastMAS 当前执行状态

- 单一主基线：`specification/20260925/CoastMAS-Codex-master-spec-20260925.md`；Planning补充已全文读取并归入W09/W12/W14/W15/W17及`acceptance-master.json`。V3四类任务、十个一级中心不变，不另写总体方案。
- 当前可访问开发构建：`http://127.0.0.1:58013/`，`artifacts/master-v3-dev25`。API/worker自有服务会话61145；58012未替换。原始资料、模型原包、历史和未提交工作保留。
- V3实际导航已纠偏；项目CRUD唯一。模型目录、数据—模型匹配、规划任务、指标库、规划目标、约束库、决策变量是真实目录/API。尚未接通的入口仅在开发环境标为开发中，无空路由。四类任务可创建，独立八步不等于完整业务通过。
- 本轮Planning已实现：独立目标集/约束集/决策定义，无PlanningProblem父对象前提；服务端草稿自动保存；ETag/If-Match、412冲突及base/server/local对比；不可变发布版本和成员表；显式应用固定版本到规划任务；发布新版本不升级任务。服务端项目权限核验；发布配置不等于业务批准。
- 已配置并有数值函数的规划基础：4项加性目标度量、3种约束、3种二元决策模板。不是完整目标/约束/诊断库；多用途配置、独立Scenario/Diagnosis、问题编译、运行候选、再评价、比较、报告仍待实现/连接，不能计为科学资料阻塞。
- 新增`planning_solver_v1.py`：真实SciPy二元MILP、最多18单元的完整精确Pareto枚举、独立约束残差、无解/超时/取消状态、权重与有依据的价值尺度校验。7项数值测试通过。**尚未接入API/队列/地图，不能算PLAN-E2E通过**；规模限制明确拒绝，不抽样替代。取消中的本地MILP仍需执行器进程终止策略，未声明实时中断或checkpoint。
- 规划单元：从受管矢量/可靠坐标表经当前任务处理节点202入队，完整读取面或候选点；保留原生几何/字段、来源/Profile快照，计算真实面积并检查重叠。原件哈希、不可变UnitSet版本及产物在完成事务中登记；不猜成本/收益/时期，不增加顶层任务。当前支持100000完整要素预算，超出拒绝而不采样；栅格单元划分仍未接通。
- 矢量成果分支修复：实际空间产物即使带下载文件也进入中央地图，不再错误套用栅格像元统计。复用共享右栏/图层管理；点查、相机/不透明度/图例/要素选择及移出/恢复状态保存到固定运行；下载哈希校验。运行产物不替换成输入资料。
- 当前规划预检明确报告工程编译服务未接通，不再引导用户填写旧版优化方法。可靠资料显示和已有空间预处理仍保留。历史旧优化算法未删除。
- 指标：62项定义，8项真实核函数，54项未安装；NDVI真实上传/自动RED-NIR匹配/固定版本/202队列/原生像元/实际栅格下载已在当前构建通过。旧Run算法升级/复现J未完整验证。
- 当前dev25验证：后端290项、前端72项、lint、类型/构建通过；新增规划单元10项及PG非超级用户/非表所有者20项通过（不代表RLS完成）。5条实际浏览器链通过：导航、NDVI、地图/Dock、规划版本、单元准备。dev25同次浏览器5项通过；此前失败和修复证据保留。证据`artifacts/master-20260925/v3-dev25/`。诊断日志不再展开数据库连接凭据；曾暴露在测试日志中的临时测试账号已禁用，原始失败证据已脱敏。
- 实测无Dock成果地图：1440×900顶部121px/高774px；1366×768顶部121px/高642px；1920、2560也覆盖。已查看实际截图。完整AHP/Planning/Comparison业务视觉覆盖未完成，不冒充全站验收。
- W01新PG/RLS/契约基础、完整ModelOps/Coupler/RunManifest/SSE/沙箱/空间并发/不确定性仍待研发。原145验收和177能力不继承旧PASS；PLAN-E2E01～05全部NOT_RUN。独立科学问题保留，但不掩盖工程缺项。
- 下一执行位置：把独立版本对象接到已实现的PlanningUnitSet、待实现的Scenario/Diagnosis与PlanningProblemCompiler；从已有受管资料及固定Result读取，不重复上传，不猜单位/阈值；再接solver到队列、CandidateState、独立约束报告和Assessment再评价。复用现有读取/空间输出/队列。继续补齐W01/W02基础、W07/W08权重UI及余下主任务。UnitSet目前按任务读取版本列表，目录分页/跨任务显式复用与完整算法包冻结仍需补齐。
