# 交付包入口

唯一执行基线：`CoastMAS-Codex-master-spec-20260925.md`。
先读第00章来源与裁决、第01—05章业务/UI、第20—28章工程与执行，随后按W任务读取对应技术章节。
完整规范在主文和附录内，不需要未提供的旧任务书。JSON是同一基线的机器可读索引，不是平行规范。

三个源文件中，第1与第3相同；source-manifest.json记录原始hash，source-traceability.json记录全部编号章节覆盖。

本包只有研发设计与规格验证，不包含已实现的CoastMAS生产代码。全部应用验收初始NOT_RUN。
运行validation-report.json列出的检查只证明文档/schema/索引一致，不证明服务器、算法、数据库或浏览器测试通过。

用户提交本包即按主文第28章连续执行委托；破坏性生产切换/删除、付费、外发或公开发布仍需独立授权。
