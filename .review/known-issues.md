# 已知问题

- 本次实际浏览器：4 通过、1 失败。规划单元透明度预期 0.6、服务端为 1；根因尚未确认。失败后的刷新/移出/恢复链未执行。不能引用旧通过日志覆盖本次失败。
- 当前 NDVI 截图中央未呈现清楚的栅格着色。数值、像元和下载断言通过不等于可视验收；须检查渲染与截图时序。在线底图也不保证完整可用。
- 大前端包警告、NumPy 等依赖弃用警告仍存在，未屏蔽。
- 五条完整 Planning 路线未完成：独立 Scenario/Diagnosis、ProblemCompiler、求解器队列、CandidateState、再评价及比较报告待接通；不是等待用户补资料。
- UnitSet 列表暂未分页/跨任务显式复用；100000 完整要素预算、地图 5000 要素显示预算有明确限制，不能冒充无限规模能力。
- 62 项指标中 54 项未安装；自定义算法发布、完整权重界面、旧 Run 固定算法重现等专项尚未全量验证。
- 新 PG/RLS、完整 ModelOps/Coupler/RunManifest/SSE/取消确认/沙箱/空间并发/不确定性等主任务仍有未实现或未验证项。现有非超级用户 PG 测试不代表 RLS 已完成。
- tests_v1/test_foundation.py 引用缺失的 coastmas_next.v1；本次不运行该套件，不记 PASS。历史前端 E2E 部分依赖本机夹具路径，跨机器完整复现仍需整理；锁文件和现有代码均保留。
- 正式业务尚缺部分指标科学定义、方向、A/B/C 含义、TN 可靠定位/解释等。原件不改、不猜填；不阻断无关工程开发。
- 原始 data_quan、模型原包、私有声明、数据库、登录凭据、浏览器 trace 与本地运行目录不上传。复用这些模型仍需合法取得原包并按安装/验证流程接入，不能把代码仓库当作私有资料备份。
- 本次仅建立 GitHub 代码管理，不宣布整体研发完成。后续优先处理当前失败，再继续 STATE 所列规划编译及平台能力任务。
