# CoastMAS 完整开发设计与验收任务书
## 上传即自动出图 · 多源综合评价 · 全流程可视分析

**设计日期：2026-09-24**  
**交付性质：供 Codex 实施的整合开发规格；本文件生成不代表产品已实现或已验收。**  
**实施边界：以最新要求建设，不承担旧页面、路由、API、请求契约、数据库结构或旧客户端兼容义务。原始资料、有效声明、模型原包与既有证据不因不兼容而被删除。**

---

## 0. 唯一执行基线与范围

### 0.1 本次最新裁决

**具有可靠空间定位的数据，接入成功后自动建立图层、生成可用预览并定位地图。普通查看不以人工描边界、识别边界、创建地理实体、建立场景、填写科学定义、选择评价方法或执行科研模型为前置。**

文件范围、有效数据覆盖、分析 AOI、统计控制区和业务对象是不同概念。系统自动读取或派生前两者；后三者仅在具体业务确实需要时引用或选择。不能把“自动出图”改造成另一个必须点击的“边界识别任务”。

保留可选的绘制、裁剪和地物提取能力，但从普通接入的前置链条中移除。用户要看的是原始栅格/矢量数据，不是仅看一个外围多边形。

### 0.2 本文合并并取代什么

本文整合下列已交付材料的有效业务要求，使实施者不必反复拼接多份任务书：

- 《CoastMAS 专业工作台纠偏方案》：业务可发现、侧栏、GeoAI 风格工作区、专业目录、模型上传。
- 《CoastMAS 综合评价业务流程与系统修改方案》：多源资料、统计空间化、GIS/遥感指标、标准化、AHP/评价型投影寻踪、综合栅格。
- 《CoastMAS 全流程可视分析与科研制图补充规格》：地图—表格—图表—过程联动、贡献解释与科研出图。
- 此前 B01—B13、W01—W08、V/VV 的有效功能目标、科学正确性及安全要求。

**冲突优先级：用户最新明确要求 → 本文明确裁决 → 已确认业务/可视化细则 → 历史设计和审计线索。**

取消历史材料中的旧结构兼容、必须沿用具体旧文件落点、必须先建实体/画 AOI 才查看、仅审计后停止等执行限制。保留历史审计原文，不追改其结论或把历史 PASS 变成新版本 PASS。

本例以栅格综合评价为主验收链，同时覆盖管理单元/矢量分析、聚类、回归、变化、空间优化、科研验证、方案协同和模型接入等既定能力。可合并入口，不能以“重设计”删掉业务。

### 0.3 证据与推断边界

当前截图可确认页面已经显示 `PRD_distance_construction land.tif` 的预览，并标示 399.42 MiB、102,744,000 像元。这些是截图中的显示信息，不是本次独立读取原文件得到的测量；也不能据截图认定当前预览已经是正确的动态瓦片、原生精度点查或全域统计。

先在当前开发工程中定向核对接入、渲染、数据层、方法层与执行器，复用正确实现。不要再次全库泛化审计，不要从截图断言后台一定不存在某项能力。用户自己的 GeoAI 工程是视觉和组件参考，不是同名公开模板，也不是运行时依赖。

本文中导航、接口、对象名、性能预算和任务划分为开发设计，不宣称是通用行业标准。标准和底层工具说明见文末来源；未提供的真实年鉴、指标判断、投影依据和独立验证资料不能由系统编造。

### 0.4 完成定义

交付一个实际可运行的新版本，而不是文档、界面原型、注册表或样例截图。完整交付同时包括：

1. 上传/接入后自动预览、定位、原生值查询与数据管理。
2. 从真实原始影像、地物资料和统计资料生产指标的实际算法链。
3. 指标标准化、真实赋权、完整目标域综合计算及贡献/质量结果。
4. 全流程可视分析、标准数据导出、科研图件和模型上传接入。
5. 清楚的权限、来源、版本、服务端恢复、资源预算和验收证据。

用户将本文作为开发委托提交后，内部连续实施，不在每个子项后等待例行批准。正式服务切换、原资料删除、额外付费、外发数据和公开发布仍需单独授权。

---

## 1. 最少填写：把工作交给系统，不把业务藏起来

### 1.1 三条日常路径

| 场景 | 用户最少操作 | 系统完成 | 不应出现的前置 |
|---|---|---|---|
| 仅导入与查看空间资料 | 选文件/目录/已授权来源 | 保存原件、识别格式、读取定位、建立图层、自动出图 | 任务命名、描边界、建实体、填单位、配模型 |
| 已有资料与认可方案做评价 | 选方案和资料；研究区/时期仅在未确定时选择 | 继承配置、适配输入、生产/装配指标、执行并出图 | 重录来源、重新写指标含义、手拼 JSON |
| 第一次处理未知统计/方法资料 | 选资料，集中处理实际缺口 | 自动提取、对照、候选映射、按适用组保存可复用依据 | 逐文件反复填同一科学定义 |

上传本身是用户要求导入的动作，不额外加“是否允许自动预览”。安全/格式检查在后台执行。昂贵全域统计或科研计算不因单纯看图自动启动。

不要求先创建命名科研任务才能浏览资料。当前任务内导入则自动绑定；数据资源页导入则进入项目资料库和查看工作区，后续“用于分析”携带引用创建任务草稿。

### 1.2 填写项的出现条件

一个普通字段只有在“当前操作确实需要、无有效来源可读取、无适用认可规则可处理、需要人作新判断”时才出现。可取得的信息只读展示来源，提供修改入口，不要求重复确认。

任务、数据、派生产物和导出文件名自动生成可编辑建议。未知概念使用中性名称 `原文件名 / band_1`，不凭文件名给出已核实的科学名称、单位或方向。

已有模板复用分为文件事实缓存、认可知识模板、本次数据绑定三层：新文件 hash 改变重新读事实，但结构/科学范围仍相容时沿用模板，新建绑定并重验；不复用旧计算值。

### 1.3 不把安全与科学严谨变成无意义阻断

缺科学定义不阻止获准资料的技术读取与原值显示；缺模型适用性阻止对应模型运行；缺公开许可阻止公开/再分发，而不是一律禁止内部获准查看。拥有访问权限与资料科学正确性分开判断。

资料质检不要求解释变量、响应变量、AHP 权重；空间聚类不强要评价优劣方向；已训练模型预测不强要预测对象的真实响应作为输入。

---

## 2. 自动定位与边界：必须区分的六个对象

| 对象 | 取得方式 | 使用场景 | 是否要求人工描绘 |
|---|---|---|---|
| 文件网格/要素范围 Extent | 定位模型、行列尺寸或矢量几何读取 | 自动定位与空间索引 | 否 |
| 有效数据覆盖 Footprint/Mask | 按真实 NoData、mask、alpha 与有效几何派生 | 透明显示、有效覆盖检查 | 否 |
| 地图视口 Viewport | 用户缩放/平移的显示状态 | 浏览和可选视口统计 | 否，且不是分析 AOI |
| 分析 AOI | 继承项目/任务、使用资料范围、选现有行政区/边界文件 | 裁剪与评价交付范围 | 可选绘制，不是默认 |
| 统计控制区 | 与年鉴统计口径匹配的可信行政边界 | 总量分配、行政汇总和核算 | 不描绘，优先标准边界及代码匹配 |
| 业务对象边界 | 来源产品、人工编辑或专用提取算法 | 岸线、湿地、建筑/地块等业务分析 | 仅对应业务按需提供 |

### 2.1 明确禁止的做法

- 把 GeoTIFF 加载上图先转成一个需要审批的实体。
- 为读取有效覆盖默认调用大模型分割或地物识别。
- 以栅格外框或有效像元足迹冒充行政区、真实岸线或土地利用类别边界。
- 为显示连续栅格先逐像元矢量化。
- 将黑色、0、负值或低数值统一当背景删除。
- 用显示足迹简化多边形代替原生 mask 做精确统计。
- 将地图可见部分静默作为后续科研计算范围。

GDAL 可读取坐标/地理变换，并能根据 nodata/mask 自动派生有效像元足迹；这类确定性数据处理不是图像语义识别。[S1–S3]

### 2.2 不需要人为边界，不代表可以猜定位

有可靠 CRS/变换、标准化地理坐标或已验证定位档案时自动出地图。定位缺失/矛盾时，原件照常登记，先显示非地理图像/表格与定位问题，不能套默认 EPSG:4326、猜测研究区或利用地名伪造范围。

提供“从伴随文件补定位”“引用有证据的坐标定义”“选择实际支持的地理校正方法”等修复入口。指定源 CRS 与重投影必须是不同动作。

RPC/GCP/地理定位数组不是普通仿射变换：仅当已支持的定位/校正链条和必要高程等依据齐全时自动生成地理预览；依赖不足显示具体原因。不能只取四角后把非线性影像贴成矩形。

---

## 3. 产品信息架构、页面模板与风格

### 3.1 分组侧栏＋唯一顶部项目上下文

| 分组 | 入口与功能 |
|---|---|
| 研究项目 | 项目概览、研究工作台；综合评价及其他任务入口 |
| 数据准备 | 数据资源、年鉴与统计空间化、空间处理工具；地理对象作为资料子类/专题入口 |
| 指标与评价 | 指标生产、标准化、权重与综合评价；按任务关联 |
| 模型与方法 | 模型与算法、指标/方法方案、标准与字典；维护与使用分层 |
| 成果与验证 | 运行记录、成果管理、科研验证、方案协同 |
| 管理 | 用户/项目/权限、已授权数据源、作业与存储健康、审计 |

知识图谱置于来源/方法依赖视图；模型静态拆解置于上传接入工具；外部数据源从数据资源可发现。不要求把每个内部对象做一级菜单，也不能通过四个抽象菜单隐藏业务。

顶栏放品牌、一个当前项目、搜索、运行通知、环境和账号。新系统自己的深链接、前后退、权限和恢复完整；不为旧 URL 建兼容层。

### 3.2 五类页面模板

1. **项目概览**：真实最近任务、需要处理的异常、成果与快捷动作；不展示虚构仪表盘数字。
2. **目录管理**：分类树＋服务端列表＋单一详情面板。
3. **空间/时间研究工作台**：中央主视图、图层/资料面板、当前步骤与对象详情、底部统计/运行。
4. **方法/模型维护**：使用目录优先，维护时表格/矩阵/接口示例；非通用长表单。
5. **运行/成果详情**：当前实际执行、地图/图表/表格、来源、比较和导出。

### 3.3 空间工作台布局

应用侧栏可收起为图标栏；任务内左侧图层/资料面板，中央地图，右侧当前环节/Inspector，下方可收起属性/图表/作业抽屉。一个地图可承载多图层，不给每张文件卡挂一个常驻地图。

地图占主要可用视口，不是埋在长页面底部的一张缩略卡。阶段切换复用同一任务上下文；年鉴核对以原表与清洗表为主，AHP 以层次/矩阵为主。

建议样式基准：顶栏约 52px，应用侧栏约 224px、收起约 56px，内容边距 16–24px，正文约 14px，目录行高约 36–40px，必要触控区不随视觉密度缩得不可操作。这些是设计起点，不是已测量事实。

在 1440×900、1366×768 等桌面分辨率下，不同时保留两层全宽侧栏而挤掉地图。右侧详情按需展开；小屏使用抽屉和中央标签切换，不让整个页面横向溢出。

### 3.4 GeoAI 与登录页参考

定向读取用户已授权的 GeoAI 工程的 theme tokens、应用外壳、Sidebar、DataTable、Inspector、地图控件和登录组件。独立复用可用样式/组件，不依赖 GeoAI 的账号、服务或数据库，不从网上随意选同名模板。

登录页采用克制的品牌区与独立登录卡；只提供真实实现的认证/恢复方式。显示密码、错误定位、键盘和密码管理器可用。成功后返回获准任务；会话失效不丢服务器草稿。不能展示通用测试密码、假 SSO 或假注册入口。

---

## 4. 接入即出图：前后端完整生命周期

### 4.1 两条并行通路

**快速查看通路：**安全接收 → 原件受管 → 格式/定位读取 → 低成本真实预览/瓦片 → 图层登记 → 自动定位与点查。

**科学准备通路：**相同资产 → 完整/抽样检查 → 有效语义复用 → 当前方法适用性 → 必要适配 → 指标/模型输入。

快速查看不等待科学模板认可、全域直方图、精确覆盖多边形、完整 COG 转换或正式业务审批。安全检查和访问授权不能跳过。

### 4.2 接收与恢复

- 支持多文件、文件夹/授权目录、已有资产和受控远程来源。用户浏览器路径不是服务器绝对路径，不能混用。
- Shapefile 及其他配套文件按逻辑数据集分组；列清缺少的配件，不把 .dbf/.prj 当互不相干的影像。
- 大文件采用有界流式接收，服务器计算 hash；不把整文件放进前端字符串或请求内 JSON。
- 支持分段/多段上传或等效的可恢复方案；单段重试和最终提交均幂等。已经成功接入的文件不因同批另一项失败而回滚。
- 先隔离暂存和安全检测，再转受管原件；超出磁盘/配额或格式安全失败如实说明。即使只读解析也置于受限工作环境。
- 同字节复用物理存储须服从权限、所有权和许可；资产关系保持独立，不能泄露其他项目已存在相同文件。
- 原目录只读；缩略图、索引、overview、COG、副车、统计和临时文件写专用缓存/产物目录。

### 4.3 自动加入工作区

用户主动添加资料是授权加入当前查看工作区的明确动作；在具体任务的“添加输入”入口，同步建立该任务输入引用。仅在目录浏览、点击行或勾显隐不改变分析输入。

接入成功后自动建立 LayerView 并打开地图/表格预览，不再要求另点“空间预览”才能看到任何内容。多图层容器自动列出全部逻辑层：有明确入口意图时打开该层，否则可用低成本概览并突出一个确定的活动层；不可静默只登记第一层。打开哪层是显示决策，不代表选择它做科学计算。

首批按正确范围自动 fit；批量后续异步就绪不反复抢用户镜头。用户已经平移后只提示新层就绪，并提供“定位本层/本批全部”。文件跨很远区域时保留全部并提示，不按经验自动排除离群文件。

### 4.4 状态独立而可读

| 状态维度 | 示例状态 | 不得互相冒充 |
|---|---|---|
| 接收/受管 | 接收中、校验中、已受管、失败、部分完成 | 已受管不等于已出图 |
| 地理定位 | 可定位、缺定位、定位冲突、定位链未支持 | 能读 TIFF 不等于能正确地理定位 |
| 显示 | 生成中、可查看、局部失败、失败 | 显示失败不自动否定原件完整性 |
| 科学就绪 | 未要求、待映射、当前方法适用、不适用 | 可查看不等于可评价 |
| 实际执行 | 未提交、排队、运行、取消中、成功、失败 | 模型成功不等于业务验证 |
| 成果适用/验证 | 未评估、工程验证、已完成指定验证、不适用 | 资料质检不必强贴正式模型验证标签 |

用户界面呈现组合摘要，例如：“已导入，可在地图查看；用于综合评价还需确认指标含义”。详细技术状态在详情中，不把所有说明堆到首屏。

### 4.5 当前截图的目标体验

对具有可靠定位的该距离 TIFF：选文件 → 原件上传/受管 → 自动显示实际栅格 → 自动适配视口 → 生成图例与原生点查。

不要求填任务名、不需要描边界、不新建管理单元、不选择模型。文件本身未声明单位时图例可显示“原始数值；单位未声明”，已有用户声明单独展示。不能凭文件名把值直接认可为米、把支撑设为管理单元或赋予优劣方向。

---

## 5. 定位、足迹与栅格显示算法约束

### 5.1 定位读取

对仿射栅格使用完整六参数和行列尺寸构造边界；有旋转/斜切时不能只用 north-up 假设。边界转显示坐标时按投影需要加密采样，正确处理轴顺序、跨日期变更线和投影适用范围。

读取文件及真实伴随资料里的 CRS、坐标单位、地理变换、GCP/RPC/地理定位数组、波段描述、scale/offset、mask、NoData、alpha、颜色表、overview 和尺寸。记录实际使用的定位链及实现版本。[S1/S2]

源 CRS、分析 CRS、底图/显示 CRS 分开。为了地图叠加的显示重投影不把原资产改写成 Web Mercator，也不意味着正式分析网格已经统一。

### 5.2 有效覆盖和范围

- 第一幅图不依赖多边形足迹：渲染直接按真实 mask/NoData/alpha 透明处理。
- 需要目录覆盖或定位优化时自动生成足迹；低分辨率概览/简化足迹必须标出精度与用途，不作为精确面积或统计分母。
- 多波段足迹的 union/intersection 与显示波段组合必须明确；显示 band_1 的有效区不自动代表所有分析变量的公共有效区。
- 精确空间统计用原生数据掩膜和选定算子，不依赖展示多边形近似。
- 无有效数据时显示真实范围与“无有效像元”，不伪造彩色面；没有 NoData 声明而像元为黑/0时不自行去背景。[S3]

### 5.3 渲染规则

| 数据 | 默认显示 | 自动化边界 |
|---|---|---|
| 有 RGB/产品映射的影像 | 实际 RGB 与适用显示拉伸 | 多个波段不自动等于真彩色；未识别时中性单波段并可选择 |
| 单波段数值栅格 | 中性连续色带或已有认可样式 | 依据实际有限样本/有效统计确定范围，显示其来源；不虚构 min/max |
| 类别栅格 | 已知颜色表/类别字典；未知则显示实际代码 | 不把类别码按连续量插值成新类别 |
| 矢量点线面 | 按几何自动符号化、范围定位、属性表 | 不先要求“地理实体类型”才能显示 |
| 地理表格 | 标准元数据/认可映射明确坐标时自动点图 | 普通 x/y、地址、地名有歧义先表格，必要时一次映射 |
| CF 多维数据 | 读取有证据的坐标轴，显示实际变量/时片 | 无经纬或投影依据时不猜；默认显示切片注明，不自动选择计算期 |
| 年鉴/普通文档 | 原文与结构化表 | 不从行政名称直接伪造网格位置 |

默认拉伸作为显示方案记录，不每瓦片独立拉伸造成接缝。提供重置、透明度、图层排序、波段和适用色带控制；默认样式可用，不强制人工制图后才出图。

### 5.4 大文件实现

采用服务器窗口读取/显示瓦片，原生 COG 直接利用有效 overview/块读取；非 COG 普通 TIFF 用有界预览尽早出首图，在后台生成受管显示缓存或优化副本。不要等精确全图统计、完整足迹和全量转换全部结束才呈现第一幅图。

COG 布局符合性与远程 HTTP Range 行为分别检验。不要凭文件可读或扩展名就宣称支持 COG。服务器、本地 object store 和浏览器各环节必须实测；反向代理丢 Range 也不能算支持。[S4]

大矢量采用分页、空间查询、矢量瓦片或经验证的等效路径；显示简化不覆盖源几何。禁止向浏览器发亿级像元数组或无限 FeatureCollection。

### 5.5 原生点查

`inspect` 从实际资产/固定结果读取，返回资产与版本、变量/时片、源或目标网格、行列/要素 ID、存储值、scale/offset 后值、单位来源、有效状态和定位链。不得从 PNG 取色反推，也不把显示 overview 值当原生值。

“原始源像元”和“重采样后评价网格像元”分别说明。多图层点查返回各自的来源身份，不用同一行列号查询不同网格。选中历史运行时所有点查固定历史引用。

---

## 6. 资料、来源与标准适配共用层

### 6.1 数据管理不是卡片墙

分类/集合树＋目录表＋单一详情。表格支持明确授权范围内的服务端搜索、排序、筛选、分页、列显隐、保存视图、跨页批选与引用查询。分类按原始资料、结构化统计、空间化、过程数据、原值指标、标准化、权重、综合成果组织。

名称链接打开详情；行单击激活 Inspector；复选框做批选；图层眼睛只控制显隐。“定位地图”“用于分析”“下载原件”“查看引用”“归档”有明确区别。显示 B/KiB/MiB/GiB，未知数量不是 0。

批量明确“本页/选中项/全部筛选”；保存筛选作用域和版本快照，避免用户批选后新到数据被意外包含。部分失败逐条保留。归档、移出集合、移出任务和物理删除分开；物理删除有权限、依赖检查与明确确认。

工程夹具按来源标签隔离，不按文件名猜测删除。默认真实工作项目不混入自动测试写入物。

### 6.2 三层复用与逐动作要求

| 层 | 保存 | 失效依据 |
|---|---|---|
| 文件事实 | 解析版本、文件/图层/变量路径、技术属性和 hash | 字节或解析规则变化 |
| 声明/语义/方法模板 | 来源、责任者、依据、概念、单位/支撑、参数、适用范围与版本 | 真正相关的口径、范围、权限/许可、方法变化 |
| 本次绑定与计划 | 精确资产、字段/波段、模板、方法、AOI/网格和转换 | 输入或相关规则变化；重新检查 |

要求解析器按动作返回缺口，至少支持 VIEW、QUALITY_CHECK、PREPARE、RUN、EXPORT、PUBLISH。`requirements(action)` 不能永远返回一个“所有科学字段必须完整”的清单。

数值编码可能合法，不能只因“科学含义”是数字就拒绝；但必须有可理解的概念定义、显示名和依据。候选相似匹配不是批准，LLM 不自动认可语义。

### 6.3 最低标准档案与处理范围

| 档案 | 本轮正向能力 | 明确边界 |
|---|---|---|
| GeoTIFF/COG | 定位/多波段/mask/实际显示/点查/计算输入/科学导出 | COG Range 单列测试；RPC/GCP 按实际定位子集 |
| GeoJSON | Geometry/Feature/Collection 导入、几何显示、字段映射 | RFC7946 与 legacy CRS 区分；Z 保留与2D投影显示边界说明 |
| GeoPackage | 版本/图层发现、多图层管理、属性/几何、业务映射与导出 | 非空间表、栅格表及第三方扩展按实际范围，不忽略未知扩展 |
| Shapefile 配套文件/ZIP | 分组、编码/CRS、字段、几何、GIS分析 | 字段截断/缺 .prj/编码歧义有具体修复；导出损失说明 |
| CSV/TSV/CSVW | 常用方言、类型、空值、前导零、字段和坐标/行政映射 | CSVW 单位语义需明确词表或扩展，不假称其自动定义所有单位 |
| XLSX 与电子年鉴表 | 表/区域读取、原位置、单位/地区匹配、可导出统计表 | 公式缓存缺失/合并表头/脚注明确；不是执行任意宏 |
| 数字PDF/HTML/文本；扫描兜底 | 有依据的表格/文字提取、原文对照 | OCR 仅无更好来源时使用；扫描精度需核对 |
| NetCDF/CF | 明确版本的轴、坐标、日历、bounds、cell_methods、packing/缺测读取与适用任务 | CF 1.12 与选定1.13能力分别测试；360_day 不硬转 UTC |
| STAC Item/Collection | 发现资产、时空和适用扩展、受控获取与固定快照 | 不等于任意远程URL可访问；扩展版本单列 |
| 方法/字典/新领域 JSON/YAML | 新 schema 的导入、验证和版本化；普通用户用映射界面 | 不承担旧私有 schema 兼容，未知结构不猜任务 |
| OGC API—Processes | 既定标准子集的描述、执行与作业/结果映射 | 取消等按符合性类核查；非认证声明，非公网默认开放 |

每档案记录规范/扩展/实现版本，分别声明识别、结构、语义、映射、任务、导出与损失。支持名单后端统一维护，前端读取，不让方法作者勾选格式来“创造”解析能力。[S7–S9]

标准检测允许明确选择其他适用档案，但不能用用户强选掩盖实际不符合；重新校验并保留依据。外部规范更新不自动改旧方法/结果。库版本、文件规范版本、资源版本、词表版本和方法版本分开。

## 7. 综合评价业务：八个可见环节，不是八次强制确认

**主线：评价设定 → 数据整理 → 空间统一与统计空间化 → 指标计算 → 指标标准化 → 权重计算 → 综合评价 → 验证与交付。**

用户可先上传看图再发起评价，也可先选任务模板。指标体系前置是针对正式评价设计，不是阻止先看数据。已有可信内容自动满足阶段；底层按依赖图执行，不按界面点击数模拟完成。

| 阶段 | 用户输入 | 系统执行 | 阶段可视与产物 |
|---|---|---|---|
| 评价设定 | 已有方案/目标；确有必要的范围/时期/单元选择 | 继承项目和资料；匹配方法与数据需求 | AOI/资料覆盖与任务摘要 |
| 数据整理 | 选文件/资产，仅处理真实提取歧义 | 自动出图、结构化统计、质量和字段读取 | 原件、清洗表、问题 |
| 空间统一/空间化 | 缺定位或新空间分配假设才确认 | 网格/投影/时间统一；行政关联与分配 | 处理前后、行政总量核算 |
| 指标计算 | 选/沿用配方和资料，处理歧义输入 | 波段、GIS、字段和实际模型计算 | 每项原值指标、配方依赖图 |
| 标准化 | 仅新增方向/评分规则需决定 | 固定评分变换与指标栈、质量检查 | 分布、评分曲线、标准化层 |
| 权重计算 | 选择认可方法；首次AHP提供判断 | 权重拟合/计算、诊断与固定版本 | 权重表、矩阵或真实优化诊断 |
| 综合评价 | 预检并执行 | 固定参数和权重全域应用 | 综合指数、贡献层、质量层 |
| 验证/交付 | 查看和选择输出；新发布决定 | 数值/可比性检查、实际图件和数据导出 | 地图/图表/报告与标准成果包 |

### 7.1 目标空间与任务范围

统一的是分析网格或明确统计单元，不是先把全部原件改成同一种格式。原始影像保留栅格，地物保留矢量，年鉴保留表，按计算节点的需求转换。

GridSpec 一次确定 CRS、完整变换、像元大小、原点、宽高、范围和有效域策略；NoData、数据类型、scale/offset 仍由各变量保留。优先选择可信参考栅格或项目方案，系统自动补齐技术参数。

仅看图时使用资料范围，不要求 GridSpec。正式计算范围依次考虑：已有任务范围、方案定义的合法全数据域、用户选定的现有边界；绘制仅是备选。没有可推定的业务范围时，提供有明确含义的候选而非逼用户描图。

多输入资料没有同一覆盖时，展示总覆盖、公共覆盖和缺口。不能把公共覆盖静默当用户研究区；方案可明确约定“全部资料共同有效域”，用户已选该方案后无需重复确认。单纯平移地图不改变该约定。

### 7.2 三种计算域

1. **交付AOI**：最后需要显示/交付的研究区。
2. **算子工作域**：距离、滑窗、密度等所需邻域或全局目标索引。
3. **统计控制域**：年鉴总量对应的完整且口径匹配行政单元。

不先裁掉 AOI 外最近地物再算距离；不把全县人口/GDP全部分配到县内一小段海岸。完整控制域分配后提取 AOI。数据不覆盖控制域时不能偷偷改变分母吸收全部总量。

### 7.3 实体不是通用前置对象

纯栅格评价无需预建管理单元实体；矢量自身几何可以直接作为 GIS 输入。有管理单元汇总、年鉴行政关联或业务对象追踪需求时，从已有资产/边界目录批量建立明确对象引用，自动读取 ID/名称/几何，必要时一次映射。

存储形式、数值源支撑、目标统计单元分开：栅格化的行政总量仍可能代表行政区，不能仅凭 TIFF 类型认定为像元独立观测。

---

## 8. 年鉴结构化与人口/产业/经济空间化

### 8.1 原始资料转统计记录

优先电子表格、CSV/HTML和有可提取内容的数字PDF；扫描且无更好来源时才OCR。提取器保存原页/表/单元格位置和原文，自动列结构化长表并提供对照。

StatisticalObservation 至少含源版本、来源位置、地区码/名称、边界版本、统计指标定义、统计时期/精度、原始值/单位、规范值/单位、数量/密度/比率等类型、统计/价格口径和状态。后台持有该结构，普通用户不写键名。

自动核查万人/人、亿元/万元、前导零地区码、总计/小计、重复、缺失与真0。出版时间不当统计时间；常住/户籍/昼间人口区分，产值/增加值/营收区分，现价/不变价/PPP区分。行政名称歧义集中处理，边界变更使用有依据的对照表，不模糊匹配后自动宣布正确。

### 8.2 先行政关联，再按任务决定是否细网格空间化

SP-JOIN 直接把统计关联到当期边界，可完成行政专题图、字段运算和单元评价。无须为每一统计指标强制做回归降尺度。

需要区内差异时提供：

| 方法档案 | 条件与计算 | 输出及限制 |
|---|---|---|
| SP-AREA | 非负可加总量，按完整控制区内有效面积/有据适宜区分配 | 明确均匀/适宜区假设的基准 |
| SP-PRIOR | 口径相容的可信人口/GDP或其他强度先验，按统计总量调整 | 模型估计，保留先验来源与误差限制 |
| SP-REG | 足够独立区域/时期样本，实际回归基线/RF等生成相对强度后约束 | 模型、训练/验证范围、空间估计和诊断 |
| SP-SECTOR | 同口径行业总量，按行业选择辅助特征和上述方法再合成 | 分行业与汇总结果，不混重复统计 |

这是本系统的方法规范化，不宣称人口、所有产业和所有经济量存在一个万能官方公式。WorldPop提供了统计总量与辅助地理资料结合的分配方法参考，但本项目仍需自己的适用性与验证。[S10]

### 8.3 辅助资料选择

- 常住人口：相容人口先验、居住/建成区和建筑等为候选，灯光、道路、服务POI作为适用辅助；低光不等于无人。
- 第一产业：按农林牧渔分别选择地类与生产资料；近海养殖和海洋活动不能一律分到陆地住宅。
- 第二产业：工业用地、厂房、企业/就业/产能等，灯光与交通辅助；总部地址不自动等于生产地。
- 第三产业：商业服务设施、建筑、就业/人口、可达性等，说明行业与POI覆盖偏差。
- GDP：优先同口径产业分项有据合成，或核查可信格网后作先验；人均GDP、密度和每格总量区分。
- 人均量、比率与占比：保留源支撑，或有据处理分子分母再计算，不能按面积直接分配百分数。

辅助产品是否已经使用同批年鉴应进入来源记录；不能又把它当独立验证真值。选方案可自动列已存在且相容的辅助资料，缺失才询问，不把所有候选设为必填。

### 8.4 总量约束

对行政单元 a 的非负可加总量 T_a，给出合法分配量级 q_ag：

`T_hat_ag = T_a × q_ag / Σ_h q_ah`

分母必须正，控制单元不得重复核算。q 若是密度，先乘网格与行政区的有效交叠面积；若已是每格数量，不能重复乘面积。跨行政边界像元保留各控制区贡献，以便逐区准确回算。

T=0、T未知、分母0分别处理；负流量/净变化不用此非负分配器。人口可输出浮点期望数量，整数化需保总量方法，不能逐格随意四舍五入。行业分项与GDP总计不相等先解释口径/舍入/修订，不静默压平。

### 8.5 回归与验证

以真实行政单元×时期构成样本，聚合辅助特征，按行政区/空间块/时期划分训练验证。拟合预处理只读训练部分。禁止把县总量复制到每个像元后随机分割成“百万独立样本”。

行政关系下推到网格是模型假设；数据太少时推荐有依据的先验/基准，不假装已经可靠训练。核算守恒与独立细尺度精度分别验收，不能在强制匹配的县总量上用 R²≈1 宣称高精度。

可视化提供原统计、辅助资料、分配强度、约束结果、回退/缺测与验证视图。点县域看到真实统计总量、回算量、差异和来源。无独立数据则明确未验证，不生成假置信区间或假精度图。

---

## 9. GIS、遥感与指标生产算子库

### 9.1 本轮实际能力

| 算子组 | 必须可真实执行 | 科学/工程边界 |
|---|---|---|
| 定位与空间准备 | 重投影、裁剪、拼接、对齐、mask、几何校验 | 区分赋CRS和重投影；不覆盖原件 |
| 栅矢转换 | 属性/类别栅格化、面积比例、分类面提取、分区统计 | 类别不平均；总量/密度/比例分开 |
| 遥感 | 波段运算、NDVI等明确配方、适用QA与时期处理 | 按产品读红/NIR、scale/offset，不猜Band身份 |
| 基础GIS | 属性筛选、空间连接、相交、融合、缓冲、距离、面积/长度 | 米/度区分；几何距离与栅格近似标明 |
| 密度/邻域 | 道路长度密度、POI计数/密度、适用核密度/滑窗 | 半径、分母、去重和边缘工作域明确 |
| 重分类 | 类别字典映射、数值区间重分类 | 未知代码保留；代码非天然优劣顺序 |
| 表格指标 | 受限表达式、条件、分组统计、比例与人均 | 单位代数、除零、重复键、缺测和口径 |
| 空间化/模型 | 第8节方法与已接入实际模型 | 真实训练/应用规则和资源预算 |

可以封装已验证的GDAL/PROJ/GEOS等，不为“全新实现”重写基础算法；但界面、worker、参数、输出和反例必须接通。展示一个菜单名不算完成。

### 9.2 指标配方驱动

IndicatorRecipe 保存指标概念、输入角色、算子依赖、表达式/参数、单位、源支撑、目标网格、时期、缺测、质量和依据。普通使用者选“计算什么”和资料，系统自动生成处理计划。

- 距建设用地距离：真实土地利用→认可分类字典→筛选建设用地→实际距离算法→指标栅格。没有必要让用户画建设用地边界；原产品已有几何/分类直接使用。
- 道路密度：道路筛选/去重→实际长度→统计区域/窗口面积→密度。道路条数不当作道路长度。
- NDVI：识别产品红光/NIR→按实际scale/offset获得正确值→QA/mask→表达式→输出。分母0和缺测有规则，不能图好看就填0。

非线性指标先计算再聚合与先聚合再算不同，配方明确顺序。距离目标可能在AOI外；滑窗用halo，全局最近距离用全局目标或等价索引。任意独立分块导致接缝不算正确。

共享中间产物按输入版本、语义、网格、算子与参数复用；修改配方只失效依赖后代。每一步真实产物自动建立图层并与指标行/计算节点联动，不需再次上传。

---

## 10. 指标标准化、赋权与全域综合

### 10.1 三种标准化分开

单位转换、模型输入预处理、评价评分变换三者分开。原值和评分结果都保留；地图色带拉伸属于显示状态，不是计算。

本例线性评分可由认可方案采用：

- 效益型：`u=(x-L)/(U-L)`。
- 成本型：`u=(U-x)/(U-L)`。

方向取决于评价目标，不能从distance/GDP等名称推断。L/U可来自固定参考或明确拟合域，两者标识不同。常量、全缺测、超范围、除零诊断清楚；不默认截断或插补。多期不得各自重估参照后冒称绝对改善。

NormalizationModel 固定指标ID/顺序、方法、单位、曲线/参照或拟合统计、拟合域/时期、缺测和超范围规则。IndicatorStack统一实际GridSpec、变量身份及mask，不能按文件排序或行号错配。

### 10.2 真实赋权能力

**AHP**：已有方案引用、标准/可映射矩阵导入、层次结构与首次必要成对判断。对角和倒数关系自动处理，不重复双向填。明确权重算法、局部/全局权重和一致性诊断；CI/RI/CR及阈值属于具体方法。少于3项、缺失矩阵和多专家规则单独处理，不伪造CR=0；不得偷偷改专家判断。

**评价型投影寻踪**：独立WeightEstimator或DirectEvaluator，开发者核对真实代码/文献，明确目标函数、方向约束、优化器、多起点/种子、样本域、标准化、权重解释与基准。PPCI聚类分割、回归系数、RF重要度或PCA载荷不能直接当评价权重。工程缺实现由研发解决，不列“用户没填”。

若真实方法只给投影方向，必须规定有依据的转换；不能任意取绝对值或平方归一化就宣称等价。若直接输出得分，走DirectEvaluator，不再二次加权。用户要求“权重→加权求和”的路径使用合法WeightSet。

等权、手工权重作为明确选择的基准保留；已有熵权能力继续正确接入，并记录拟合域。新增其他方法不能只列下拉名称而无执行器。AHP和评价型PP不以仅一个演示算法替代。

### 10.3 权重不是同一概念

人口/GDP空间分配份额、回归/投影内部参数、综合评价权重分别命名与存储。WeightSet保存指标ID、顺序、权重、方法/判断或拟合数据、目标、范围和版本；本例约定非负且和为1，不能推广为所有外部模型要求。

### 10.4 全域计算

`S(g)=Σ_j w_j u_j(g)`；`C_j(g)=w_j u_j(g)`。

默认在所有必需指标共同有效、且属于声明研究域的单元求值。缺一层不填0，不暗中重新分配该像元剩余权重。确需另一缺测政策，作为独立方法明确输出有效权重覆盖和可比性影响。

全域固定NormalizationModel和WeightSet，分块只应用不重新拟合。不把10000上限调大当算法全域实现，也不每块独立训模型后拼接。

PPCI与pprRFA已有样本算法可以复用到新接口，但全图赋类/预测要检查原实现是否提供合法应用规则；只有验证相应规则后才开放全域，不用其他算法冒名替代。训练与预测是不同执行节点。

### 10.5 本例核心解释与算例

标准化分数 `[0.4,0.6,0.8]`、权重 `[0.2,0.3,0.5]`，贡献 `[0.08,0.18,0.40]`，综合 `0.66`。这是可手算验收夹具，不是真实PRD结论。

点选综合图原生目标像元，右侧返回原指标值、标准化值、权重、贡献、综合值、mask和来源。贡献和与文件结果相符，不能从渲染颜色取值。

---

## 11. 全流程可视分析与科研制图

### 11.1 默认显示内容

原始资料、空间化、过程数据、原值指标、标准化、贡献、综合、质量分组。当前步骤自动打开主图与必要参考层；其余在图层树可见，不全部同时覆盖。

| 环节 | 必需视图/交互 |
|---|---|
| 数据接入 | 自动定位/图例、RGB或单波段、矢量属性、年鉴原文与表格 |
| 空间处理 | 原/目标网格和范围、前后卷帘或并排、有效区与误差诊断 |
| 统计空间化 | 行政原统计、辅助资料、强度面、约束结果、逐区核算、真正独立验证 |
| 指标 | 地图、分布、源数据/目标地物、配方/运行节点及实际输出 |
| 标准化 | 原值分布、评分曲线、标准化分布、异常/缺测与相关性诊断 |
| 权重 | AHP层次/矩阵/一致性，PP真实拟合信息、权重图、可复现敏感性 |
| 综合 | 综合地图、贡献图、原生点查、覆盖与有据行政统计 |
| 比较/交付 | 同期多方案/多期对照、合法差值、科研版式与图集 |

### 11.2 联动与状态

地图—图表—表格—流程用稳定身份联动：栅格为版本/变量/时片/GridSpec/行列，矢量为版本/图层/要素ID，统计为边界版本/地区码/指标/时期/来源记录。不依赖表格行号。

切换图层或历史运行时丢弃过期异步响应。地图点查、表格、图表和下载同一版本。窗口/样本/选区/全域统计分别标记，未知分母不填0。大规模高亮返回服务端选择引用或掩膜层，不传亿级ID。

相关性、残差、收敛、置信区间、敏感性等只能显示真实计算得到且适用的内容；没有独立验证则不画假精度。图表过滤不改变正式输入和AOI。

### 11.3 科学制图规则

图例自动填变量、单位来源、时期、值域/分段、拉伸/统计域、缺测和版本。未知单位可以原始数值预览，不冒填。类别、连续量、正负差值选不同适用默认色带，不用唯一一套深紫色填所有内容。

0、NoData、未覆盖、未计算和失败以文字/符号/纹理辅助区别，不只靠颜色。[S11]

同指标跨期/方案先核可比性，再共用数值色标。原值与标准化使用各自单位和图例，不强锁同一范围。地图比例尺和方向来自实际投影/视角，不能手贴不随地图更新的标尺。

“优/良/高风险”等业务等级只有固定方法阈值才显示；没有依据时连续评分或纯显示分位方案，标清不代表正式等级。

### 11.4 比较与敏感性

支持同步并排、卷帘及实际差值图。单位、指标定义、时间支撑、方法、标准化基准、空间单元/网格和共同有效区不相容时允许独立图例并排看，但禁自动数值差异/排名宣称。

权重或参数调整进入“敏感性试验”独立配置和实际作业，保留原正式结果；样本试算清楚标范围。拖滑块不能直接覆盖正式WeightSet。可复用固定指标栈实现快速试算，不必重跑原始接入。

### 11.5 科研出图

内置单指标、综合图、同指标两期/两方案、指标小多图、行政区图集、权重/验证图模板。图名、时期、图例、单位、来源和必要方法说明自动带入，用户只选版式/尺寸或必要标题。

输出实际PNG和可用PDF/SVG，保留版式与引用的运行/样式/统计版本。SVG/PDF中的栅格仍可为嵌入图像，不能声称所有数据变为矢量；300dpi仅是图件选项，不提升分析分辨率。[S12]

底图/原始资料是否可再分发按许可判断，不自动打包受限原件。字体可用于授权环境渲染，不把服务器字体文件一并分发。导出本地文件不等于公开发布。

---

## 12. 模型与算法接入：独立入口，真实可执行

### 12.1 产品职责

“模型与算法”默认目录：使用工具、上传模型/算法、接入已授权服务。普通使用不先创建端口/运行配置；维护者建立新版本，管理员按明确资格批准。指标与方法方案不充当代码上传页面。

方法定义、算法实现、输入数据语义、本次运行绑定四者分开。模型签名描述形状和接口，不自动证明单位、科学概念和适用范围。

### 12.2 本轮接入范围

必须完成真实R源码包、明确Python包/脚本接入，以及项目已有PPCI/pprRFA的新接口运行与验证。模型权重+配置、OCI镜像和受控标准服务按实际选定支持档案实施；已有范围内的服务互操作不能用静态声明替代。不能承诺任意脚本/权重上传即安全可用。

上传未登记的新R和Python示例包分别进行正向验收，不以预置两个模型能跑证明通用上传已实现。包无需包含通用国际“算法JSON标准”；系统可生成自己的版本化描述，明确标为内部规范。

### 12.3 生命周期

暂存 → 静态检查与来源/许可读取 → 接口/依赖/示例映射 → 获准隔离验证 → 实际数值/产物对照 → 指定版本发布 → 任务使用。

上传阶段不安装、不import、不执行、不反序列化不可信对象。安装钩子、R构建与模型加载也属于代码执行，必须在受限运行环境：非root、资源/时间限制、输入只读、隔离输出、默认无外网、无宿主凭据/任意目录挂载。许可/验证/发布角色分开。[S6]

审批绑定代码、权重、依赖/镜像摘要、接口和验证记录，内容变更产生新版本；技术可执行与当前输入适用及正式业务验证分别记录。

### 12.4 完整界面

概览、用途、输入输出与真实示例、参数、源包/权重、运行环境、来源许可、版本、验证、使用记录。失败说明在哪一步、缺哪个内容、谁应处理；依赖未装/适配器未写由开发维护者解决，不要求用户编造科学字段。

---

## 13. 其他既定任务不能在本例中丢失

| 任务 | 必备能力与限制 |
|---|---|
| 资料质检 | 自动读取与质量结果；不要求评价语义或模型，问题不妨碍其他正常文件 |
| 空间聚类 | 合法特征/样本与全域规则、实际簇分布；不把簇编号直接当等级 |
| 回归训练/验证/预测 | 实际输入、响应、划分、预处理、固定模型与新输入预测；不把拟合当验证 |
| 时间适配/变化 | 从资产读取值与时间支撑，合法聚合/插值/比较；非公历和缺测有明确边界 |
| 矢量/管理单元分析 | 从图层批量建立对象/关联，稳定ID、适用有效期、分区统计；不逐条上传几何 |
| 空间优化 | 候选从资料映射，预算/目标新选择、必要硬约束、真实优化与约束核验；无关字段不出现，缺成本不填0 |
| 智能编排/高级流程 | 确定性模板优先，必要时获准LLM候选；后端重验；高级画布与节点来源可查 |
| 科研验证 | 真实重复/试验/失败与用量；规划有效性与科学模型精度不混同 |
| 方案协同 | 真实目标/约束、引用结果、版本、评论、授权审核/发布；不自动决定政策 |
| 来源/知识关系 | 基于真实版本/依赖与证据，候选映射不冒认可 |
| 项目与管理 | 登录、账号、项目、权限、归档、审计和已授权连接器；后端授权不依赖按钮隐藏 |

依然允许独立算法/工具使用，不强迫每项操作走综合评价八阶段。后续可通过标准接口对接GeoAI，但本系统独立可部署和使用。

## 14. 运行、草稿、记录与错误恢复

### 14.1 任务配置与运行分离

TaskDraft可编辑，TaskRevision为一次明确冻结配置，Run为实际执行尝试，ResultBundle为固定产物。显示“任务配置第4版”“运行编号…”“模型/方法版本…”，不全用无说明vN。

“预检并运行”执行：冻结当前配置 → 服务器重查权限/依赖/单位/网格/时间/方法适用 → 固定manifest → 幂等登记作业。采用事务性作业记录与可靠入队（可使用outbox或等效机制），避免数据库写成功队列丢失。

同次点击重试用同一Idempotency-Key；同键同请求返回已建对象，同键不同请求冲突。用户主动再次运行产生新意图键，允许独立复跑；配置hash仅用于缓存/复用提示，不做永久唯一约束。

### 14.2 持久恢复

服务端草稿保存步骤、资源引用、必要小量编辑、待处理条件及布局引用，使用revision/ETag乐观并发。只有收到服务器保存确认才能显示“已保存”；离线未同步明确显示，关闭前提醒真实丢失风险。

恢复时校验权限与固定源版本，源已变不静默升级；只使相关映射/适配/预检失效。新旧冲突显示差异，不覆盖他人修改。密码、token、连接器秘密、原文件字节、巨量矩阵不进入草稿或前端日志。

文件句柄失效且上传未完成时，仅要求重选同文件并核对身份/分段校验，已保存声明不重填。完整受管后关闭浏览器再开应可重新出图，无需再选原文件。

### 14.3 作业可靠性

排队、运行、取消中、取消、成功、失败和远端未知分开。取消必须等待执行端确认，停止写入/终止进程后才标结束。失败重试引用固定输入；修改配置属于新运行。

并发任务有CPU/内存/磁盘/时间/临时文件预算，按算子类型调度。重启以租约/心跳恢复，幂等提交产物，避免两worker同时发布两份结果。部分阶段产物可恢复，不把部分结果当完整成功。

### 14.4 目录与状态文案

运行目录有类型、配置、数据/方法摘要、范围模式、状态、开始/耗时和操作；成果目录有来源运行、地图/表、验证、范围和导出。导出统一一个主入口，列实际可得的格式与内容，不摆两个无说明“完整成果”按钮。

信息示例：

- “已导入并显示。单位未声明；仅影响需要物理单位的分析。”
- “定位信息不一致。原始资料已保留；可查看图像，地理叠加待修复。”
- “资料检查完成，发现2项问题。”
- “计算完成，当前为工程验证结果；独立业务验证尚未完成。”

错误载荷含稳定错误码、对象/字段/阶段、实际原因、影响动作、可恢复性和修复路径。无数据、无匹配、无权限、加载中、服务失败分开，不全部显示空表。

---

## 15. 后端模块与数据模型

### 15.1 推荐部署形态

以一个模块化应用和独立worker为主，不为每个逻辑对象新建微服务。前端可继续采用已可用的React/TypeScript和MapLibre；后端可继续采用FastAPI；目录/权限/任务与空间索引优先利用已有可用的PostgreSQL/PostGIS，对象存储保存原件/产物，队列执行重处理。库/驱动按实际环境固定版本。

这些是成本可控的推荐，不是旧兼容约束；确有依据可替换。地图可视为投影，不作为第二份科学真源。库内保存元数据/索引/引用，大矩阵与栅格不塞进普通JSON数据库列。

### 15.2 逻辑模块边界

| 模块 | 职责 |
|---|---|
| Identity/Workspace | 登录、项目权限、成员、环境和审计 |
| Ingestion/Catalog | 接收、暂存、检测、受管原件、目录/引用 |
| GeospatialView | 定位、LayerView、显示缓存、瓦片/表预览、原生点查 |
| Standards/Semantics | 档案、声明、词表、适配计划、逐动作缺口 |
| StatisticsSpatialization | 年鉴提取、行政绑定、辅助建模、约束分配 |
| Processing/Indicators | GIS/遥感/表格算子、指标配方、栈与中间缓存 |
| Evaluation | 标准化、权重模型、综合与敏感性 |
| ModelRegistry/Runner | 源包、签名/环境、受控验证、发布与实际执行 |
| Task/Execution | 草稿、冻结、DAG、预检、作业、取消恢复与manifest |
| VisualAnalysis/Export | 图表统计、联动、成果、版式、标准导出 |
| Research/Collaboration | 独立试验、比较、评议与授权发布 |

### 15.3 最小逻辑对象

| 对象 | 核心身份与内容 |
|---|---|
| Asset/AssetVersion/Blob | 项目权限、版本、原字节hash、类型、位置引用、大小 |
| DatasetPart | 一个包内的图层/变量/时片，路径、尺寸、字段、原生类型 |
| SpatialReferenceInfo | CRS/定位方法/参数、来源、检测状态、适用与冲突 |
| CoverageInfo | Extent、mask引用、footprint/精度与用途、波段组合 |
| LayerView | 数据固定引用、活动变量/时片、样式、图例、显示状态、extent；不存科学新值 |
| Declaration/SemanticTemplate/Binding | 概念、单位/支撑、来源、责任者、作用域与版本 |
| FormatProfile/AdaptationPlan | 标准阶段能力、转换步骤、损失/确认、源目标与实现 |
| EvaluationPlan/GridSpec/UnitSpec | 任务目标、分析/工作/控制域、固定网格或单元集合 |
| StatisticalObservation/BoundaryBinding | 来源位置、地区/时期/指标、值/口径、对应边界 |
| IndicatorRecipe/Stack | 算子输入与规则、固定指标身份/顺序和数据引用 |
| NormalizationModel/WeightSet | 曲线/统计与拟合域、权重/判断、固定方法 |
| ModelPackage/Release/Validation | 真实代码/权重/环境/接口及技术和审批证据 |
| TaskDraft/Revision/Run | 用户编辑、冻结配置、操作意图、执行状态与尝试 |
| ResultBundle/Manifest | 不可变产物引用、行数/范围/单位/模型/方法与hash |
| StatisticsQuery/SelectionRef | 查询范围/定义/分母、结果/抽样、稳定选集 |
| ViewState/LayoutSpec | 个人/项目视图、图层显示、图件版式和引用，不改科学配置 |

对象不要求一对一建表。原始事实、用户声明、科学数据、显示缓存、统计缓存和操作状态职责分开；每项权限与血缘有统一实现。

### 15.4 缓存与失效

分析缓存：输入内容/版本、语义模板、算法/镜像、参数、网格/范围/时间、mask规则。显示缓存再含显示档案、band/time、投影、zoom/tile与样式。统计缓存再含统计定义、查询域、抽样规则与分母。

修改样式不触发科研重算；改变方法才失效相关后代。源对象受管后按不可变身份验证，不在每次瓦片请求重复全文件SHA扫描。对象替换必须新版本。权限/URL过期时重验读取资格，缓存命中不跳权限。

---

## 16. 新API设计与前后端契约

以下路径是本轮建议的新契约，不是当前已存在的接口；实现者可在现有工程风格内统一命名，但必须保留职责。API从一个schema生成前后端类型，不再维护多套支持清单。所有请求校验项目和引用权限。

| 接口组 | 示例新接口 | 职责/验收 |
|---|---|---|
| 接入会话 | `POST /api/v1/projects/{p}/ingestions` | 自动生成批次；无需科学字段或边界ID |
| 文件接收 | `POST /ingestions/{s}/items`；分段状态/完成接口 | 文件/分段幂等，有进度与校验 |
| 检测结果 | `GET /ingestions/{s}` | 每文件受管、定位、显示与缺口状态 |
| 资产与目录 | `GET /projects/{p}/assets`；`GET /assets/{id}/versions/{v}` | 服务端搜索/排序/分页、固定原件与详情 |
| 图层描述 | `GET /assets/{id}/versions/{v}/layers` | dataset part、实际定位、extent、样式、legend、preview状态 |
| 渲染 | `GET /layers/{id}/tilejson`；`GET /layers/{id}/tiles/{z}/{x}/{y}` | 版本固定、项目授权、按需读取和真实透明mask |
| 表预览 | `GET /assets/{id}/versions/{v}/records` | 有界行和真实字段/来源；全量计数独立 |
| 原生点查 | `POST /layers/{id}/inspect` | 真实原生值/身份/scale/mask，不从颜色反推 |
| 范围与统计 | `POST /statistics`；`GET /statistics/{id}` | 明确域/变量/方法，昂贵任务异步、可取消 |
| 任务草稿 | `POST /projects/{p}/tasks`；`PATCH /tasks/{t}/draft` | 版本并发，自动保存引用和必要编辑 |
| 逐动作缺口 | `GET /tasks/{t}/requirements?action=...` | 不把所有科学字段当VIEW必填 |
| 转换计划 | `POST /adaptation-plans`；认可/执行接口 | 确定适配自动，有损与新判断绑定确认 |
| 指标与方法 | `/indicator-recipes`、`/method-profiles`、`/weight-sets` | 真实定义/导入/版本/引用，角色限制 |
| 年鉴与空间化 | `/statistical-tables`、`/boundary-bindings`、`/spatializations` | 来源、边界映射、分配/验证与实际产物 |
| 模型接入 | `/model-packages`、`/model-validations`、`/model-releases` | 暂存不执行，验证和发布分别授权 |
| 预检与运行 | `POST /tasks/{t}/runs` | 冻结→重验→可靠入队，操作幂等 |
| 运行控制 | `/runs/{r}`、`/runs/{r}/cancel`、`/runs/{r}/retry` | 真实状态/尝试/固定配置 |
| 成果/解释 | `/results/{r}`、`/results/{r}/explain` | 图/表/点查/贡献同次运行 |
| 导出 | `POST /exports`；`GET /exports/{id}` | 数据/图件分轨，有实际文件与失败记录 |
| 标准服务 | 经选定OGC标准路径暴露能力/过程/作业/成果 | 与原生API相同权限、执行与来源，不默认公网 |

通用字段：`schema_version`、对象/项目身份、请求/操作ID、固定引用、状态及来源。错误不返回秘密、本地任意路径或其他项目存在性。

显示接口返回 `extent_source`、`georeference_status`、`footprint_kind/accuracy`、`value_domain`、`unit_source`、`statistics_scope`，不能只有图片URL和一个笼统“已验证”。

`inspect`、图表查询和导出固定版本；编辑界面可以追最新，但不能把历史运行自动替换成最新数据。长作业用事件流或有界轮询；地图瓦片不依赖不断拉全任务JSON。

---

## 17. 安全、性能与可观测性

### 17.1 安全不可缩减

上传魔数/驱动检测、归档路径穿越、压缩膨胀、文件大小/数量、解析CPU/内存/时间和磁盘配额检查。软链接/服务器目录仅在明确白名单规则内访问，不能让浏览器传任意本机路径。

不仅源码包：VRT、CSVW、STAC、XML/JSON等可能包含外部引用。禁止解析器自行访问任意URL、内网、实例元数据服务或宿主文件；按批准来源代理，限制重定向和二次请求。[S6]

模型隔离验证不得带数据库超级用户、对象存储总凭据或宿主Docker控制权限。任务数据、瓦片、缩略图、mask、统计、导出和缓存均做访问控制。需要临时URL时约束对象和有效期，不只在页面隐藏链接。

库版本/依赖/镜像固定并留清单。私有数据不因自动标准识别或绘图发送给外部LLM/地理编码/可视化平台。浏览底图请求与业务数据外发区分，遵守用户权限和提供方规则。

### 17.2 性能目标与测量口径

以下是开发目标，尚未实测。实施初始固定参考机器、内存、磁盘、浏览器和网络，在不缩小测试数据的条件下测量；未达标要说明瓶颈与优化，不伪称已验收。

| 操作 | 初始目标/限制 |
|---|---|
| 本地轻量状态/目录交互 | 正常目录p95约1秒内，加载状态即时可见 |
| 已有显示缓存首屏 | 初始数据层约2秒内可见，不包括互联网底图失败 |
| 本地受管大TIFF初次可用预览 | 以截图规模代表数据，争取20秒内显示真实初图；不能等完整科学处理 |
| 点查原生值 | 本地正常负载p95约1秒内；超时可取消并有明确提示 |
| 瓦片/地图 | 冷/热分别测，界面可操作；切层取消过期请求，无重复全文件传输 |
| 精确统计/全域计算/出图 | 异步、有阶段、有取消/恢复和资源预算，不承诺任意规模秒完成 |

分别记录上传传输、原件提交、安全检查、定位读取、第一数据层可见、显示缓存完整、全域统计完成；不能将上传网络时间藏起来，也不能把底图加载当数据层加载。

102,744,000像元、实际约399MiB资料作为代表读取案例；实际尺寸/大小以读取结果记录。批量真实资料采用可控并发，交互瓦片/点查与重计算分开配额，防止全域任务拖死目录和地图。

预览最大输出尺寸/抽样数、每worker内存、临时磁盘、单任务时限、上传配额全部配置化并由服务器强制。旧64MiB/10000行等限制不作为新算法默认正确性边界，也不能简单无限放大。

### 17.3 监测

记录阶段耗时、输入输出规模、读取字节、缓存命中、内存峰值、有效/无效分母、取消原因和产物hash，不记录凭据/敏感原文全量。已完成阶段由实际产物证明；无真实分母不画假百分比。

---

## 18. 成果目录与可复现交付

建议每次综合评价生成以下逻辑成果组；目录名称可调整，实际内容不能省：

| 成果组 | 必需内容 |
|---|---|
| 统计整理 | 清洗表、来源定位、地区/时期/口径与边界匹配 |
| 统计空间化 | 数量/密度、行政贡献、核算/验证、辅助资料与模型记录 |
| 原值指标 | 全部实际指标、单位/支撑/时期、必要中间矢量和配方 |
| 标准化指标 | 每项评分栅格、NormalizationModel、有效mask |
| 权重 | WeightSet、AHP矩阵/诊断或PP拟合规则、样本范围 |
| 综合 | 综合指数、各项贡献、可选有据等级 |
| 质量/验证 | 每层缺测、共同有效域、边界/回退标记、敏感性与验证 |
| 科研图件 | 实际PNG/PDF/SVG、统计图及版式/视图说明 |
| 来源清单 | 输入输出hash、GridSpec、变量顺序、模型/算子/档案/方法版本、参数与使用限制 |

科学栅格采用真实GeoTIFF/COG，不以颜色化PNG代替；行政/地物输出GeoPackage，表格CSV及实际CSVW或清楚的副车语义，时序按已支持CF导出。内部manifest是本系统规范，不冒称通用国际标准。

原文件下载、标准数据导出、图件输出分开命名。完整下载不能只是当前页、样本或前10000行；数据量大异步打包，可核对完整规模、mask、坐标和hash。

若许可不允许再分发原件，仅附来源引用和可发布的派生成果。公开链接和平台发布是独立授权动作，不因点击“导出”默认开放。

---

## 19. 实施任务与依赖（一次委托，内部自主推进）

用以下I任务作为执行索引，合并此前B/W/V目标，不再另写同义总体设计。只定向读取当前实现，复用正确模块。每条竖向能力同时包含前端、接口、真实处理、保存、可视和测试，不先造空框架等后续再填。

| 任务 | 主要交付 | 前置 | 继承的目标 |
|---|---|---|---|
| I01 基线与契约 | 新环境/权限/存储与测试隔离；最小资产/图层/草稿身份和档案 | 无 | B01/B05a/B04a |
| I02 上传即看 | 流式/恢复接入、定位、真实图层、首图、mask、原生点查 | I01 | B02/W01/V01；本次最高优先 |
| I03 专业外壳与目录 | 侧栏/登录/工作区、服务端目录、图层、草稿与记录 | I01；与I02联调 | B03/B09/B12/W07 |
| I04 多标准与语义 | 各档案正反例、阶段支持、声明/模板/适配和失效 | I01/I02 | B04/B05/W02 |
| I05 GIS与指标生产 | 网格/单元、算子、指标配方和阶段可视化 | I02/I04；I03外壳 | B06/W01/W03/V02/V04 |
| I06 年鉴与空间化 | 提取/行政匹配、AREA/PRIOR/REG/SECTOR、守恒/独立验证 | I04/I05 | W02/W04/V03 |
| I07 模型上传发布 | R/Python包、受控验证、新版本发布与使用，现有两模型核对 | I01/I04；I03目录 | B10/模型上传目标 |
| I08 评价与全域 | 标准化、AHP/评价型PP、固定权重、综合/贡献、全域应用 | I05；PP/模型依赖I07 | B07/B08/W05/W06/V05–V07 |
| I09 可视分析与科研制图 | 跨视图联动、比较、敏感性、标准导出、实际图件 | I02基础，随I05/I06/I08接产物 | B11/W06/W07/V/VV |
| I10 其他任务与服务 | 时间/变化/聚类/回归/优化/高级编排/科研/协同/标准接口 | 按具体算子依赖I04–I08；不互等全套 | 其余B06–B13与原业务范围 |
| I11 完整回归交付 | 全部验收、干净部署启动、真实资料流程、操作/标准矩阵 | 全部适用I任务 | B01-final/W08/VV01–16 |

I02与I03先做“上传一个已定位真实TIFF→自动出图→点查→刷新恢复”的完整路径，无任何人工边界。此为内部起点，不是只交付地图Demo的授权。已有单项产物即可同步完善I09，不等所有重算法结束。

任务存在模型或标准语义依赖时记录明确子项，维护无环图；不得再次出现接入与标准全量相互等待。需要补充的算法专项设计在当前任务内完成并验证，不把技术决策逐条抛给用户。

旧兼容测试按取消的需求退役，保留仍有效的科学/安全/可靠性测试并接到新契约。旧页面数量、字段数量、URL和JSON字节一致性不是新系统验收目标。

---

## 20. 验收矩阵：没有证据不能写PASS

### 20.1 自动出图与空间正确性

| ID | 用例 | 必须证明 |
|---|---|---|
| A01 | 已定位真实大TIFF首次上传 | 无手画边界、无建实体/方法/场景前置；自动图层+定位+实际栅格 |
| A02 | 只有文件/定位完整、单位和概念未知 | 仍可查看原始值，未知单位明示；不猜科学意义 |
| A03 | 当前距离TIFF代表规模 | 读取实际尺寸/hash；有界显示与原生点查；不是仅显示外框 |
| A04 | 旋转/斜切/半像元错位栅格 | 完整变换与定位正确，多输入不按行号错叠 |
| A05 | 0/负值/alpha/NoData/多波段mask | 合法值不被删除；图例/点查/文件一致 |
| A06 | 多岛、内部洞、全部NoData | 保留真实mask；足迹不被当真实岸线，空数据不伪填 |
| A07 | 缺CRS/定位冲突/TN类异常 | 原件保留、非地理预览可用；不默认4326或猜范围 |
| A08 | 有支持链的GCP/RPC/地理定位数组 | 真实定位路径和必要依据；不支持组合明确说明且不假定位 |
| A09 | 多图层矢量与跨远域资料 | 全层登记与可发现，自动概览不丢层，不反复抢视口 |
| A10 | 跨日期变更线/投影边界 | 正确范围或明确显示能力限制，不把局部铺满全球 |
| A11 | COG与非COG | 布局和Range分别测试；普通TIFF不因非COG无法初步查看 |
| A12 | 地图缩放/图例/视口与点查 | 显示变化不改分析hash；原生值不来自低分辨率图取色 |

### 20.2 接入、标准、数据与恢复

| ID | 用例 | 必须证明 |
|---|---|---|
| A13 | 接入中断/同意图重试/半批失败 | 同段幂等、成功项保留、草稿声明不重填 |
| A14 | 上传后刷新/关闭重开 | 原件已受管则不用再选文件；视图/引用和有效草稿恢复 |
| A15 | 同hash不同声明/权限/项目 | 不自动合并业务身份，不泄露其他项目存在性 |
| A16 | CSV/TSV方言和CSVW | 分隔符、引号、BOM、空值、前导零与语义引用正确 |
| A17 | GeoPackage/Shapefile/GeoJSON | 多层/配套/编码/几何真实映射，有损或不支持扩展有依据 |
| A18 | CF1.12和选定1.13子集 | calendar/bounds/cell_methods/grid_mapping/packing未丢；360_day不伪UTC |
| A19 | STAC与远程引用 | 真资产关系/时空/扩展与固定读取，禁止SSRF/越权与秘密外发 |
| A20 | 新批次与源/方法/许可变化 | 重读事实、新绑定、复用认可知识；只失效真正相关内容 |
| A21 | 标准支持正向矩阵 | 每个承诺阶段有成功样例及反例，不能只识别或只会拒绝 |
| A22 | 大目录/跨页批选/引用 | 实际规模搜索排序正确，选择范围明确，操作不按当前页冒全部 |

### 20.3 年鉴、指标、权重与数值

| ID | 用例 | 必须证明 |
|---|---|---|
| A23 | 年鉴电子表与数字文档 | 原文位置、地区/时期/单位和真实值保持，不逐行重录 |
| A24 | 合计小计/区划变更/代码前导零 | 不重复求和，不猜边界，对照和异常可追溯 |
| A25 | 人口分配T=100,q=[1,2,1] | 输出[25,50,25]；多行政区分别核算，不只总和 |
| A26 | AOI只覆盖县内海岸及边界像元 | 完整控制域分配后裁剪，保存跨区贡献，不强塞全县总量 |
| A27 | T未知/真0/q全0/密度或每格量 | 不错误填0；不重复乘面积；失败或认可回退标明 |
| A28 | 回归空间化训练/验证 | 独立行政/空间划分，不复制县总量造像元独立样本 |
| A29 | 守恒与独立验证 | 两类指标分开，无独立数据无假R²/置信区间 |
| A30 | 原始影像计算指标 | 实际波段/scale/offset/QA公式对照，不只加载成品NDVI |
| A31 | 土地利用/道路计算距离与密度 | 真筛选/缓冲/距离/密度/重分类；AOI外地物和块边缘不漏 |
| A32 | 栅矢转换与不同数值语义 | 类别合法、总量守恒、比例和面积统计正确 |
| A33 | 标准化常量/超界/缺层/跨期 | 真实规则与诊断，无暗中截断/填0/改参照 |
| A34 | AHP合法/矛盾矩阵与退化阶数 | 真实权重与适用一致性；不篡改专家输入 |
| A35 | 评价型PP与PPCI聚类 | 不互相冒用；真实目标/规则/数值基准及权重或直接得分 |
| A36 | 综合0.66夹具 | 文件、点查、表格和贡献图一致；指标顺序变化不错配 |
| A37 | 小图整算与分块、大图全域 | 固定预处理/权重，位置/mask/数值按预置容差一致，完整产物 |

### 20.4 模型、可视化、产品和安全

| ID | 用例 | 必须证明 |
|---|---|---|
| A38 | 新R和Python算法包分别上传 | 未预登记包→静态检查→隔离验证→发布→分析者真实运行 |
| A39 | 缺依赖/不安全包/未经批准版本 | 普通上传不执行；失败保留，权限不被绕过 |
| A40 | 地图—表格—图表—流程联动 | 稳定身份和版本正确，异步旧响应不能污染新图 |
| A41 | 样本/视口/选区/全域统计 | 分母和定义清晰，不能当前页冒全域 |
| A42 | 比较与敏感性 | 可比性核对、共用合理色标、真实差值，原方法/成果不覆盖 |
| A43 | 数据导出与科研图件 | 实际打开GeoTIFF/COG/矢量/CSV与PNG/PDF/SVG，元信息/图例/来源正确 |
| A44 | 目录/任务/运行/成果管理 | 业务可找到，当前环节明确；不是卡片墙或长页找历史 |
| A45 | 资料质检与其他任务分支 | 不要求无关字段；优化/时间/变化/科研/协同不被新例子删掉 |
| A46 | 草稿并发/会话失效/撤权 | 无已保存内容丢失，无越权草稿/瓦片/缓存/导出访问 |
| A47 | 双击/重试/主动复跑/取消恢复 | 同意图不重复，主动复跑允许，worker和发布状态真实 |
| A48 | 测试夹具、服务与原件保护 | 无写入真实项目的未说明夹具，不改原件或删历史 |
| A49 | 视口/键盘/错误/底图离线 | 三视口正常，焦点/标签可用，底图失败不冒数据丢失 |
| A50 | 真实混合源全流程 | 原始遥感+地物+年鉴+边界→空间化→指标→赋权→全域综合→解释/出图 |
| A51 | 性能与标准服务 | 参考环境冷/热首图/查询/RSS真实测量，标准API实际执行与原生结果一致 |
| A52 | 同构建完整回归和干净启动 | 后端/前端/契约/安全/浏览器/数值测试与启动说明，未运行如实 |

### 20.5 容差、对照与证据记录

类别、ID、掩膜和无损字节要求精确一致；浮点采用 `|x-y| <= atol + rtol*|y|`，依据数据类型、算法与尺度在运行测试前固定阈值。不能看到错误后放大容差求通过。总量核算按统计单位和实现浮点误差分别记录；近似方法说明误差而非冒称精确。

每项至少记录：代码/构建、输入文件和方法版本/hash、角色、用户步骤、前端请求/后端有效值、运行或作业ID、原生输出/统计/图层/图件、规模/范围/分母、数值基准/容差、失败恢复、截图/命令退出状态及耗时/RSS。

图像截图只证明所见，不证明数据正确；HTTP200、模块导入、卡片显示和下载按钮不证明功能完成。真实底图/外部标准服务不可达可用本地受控规范服务验证协议，但真实外部端点状态单列NOT_RUN/BLOCKED。

资料尚缺年鉴、边界或认可方法时，工程用标记清楚的可手算夹具继续；正式业务结论按相应条件BLOCKED，不能全体工程跟着停止，也不能拿夹具冒充真实混合源验证。

工程PASS/FAIL、业务PASS/FAIL/BLOCKED、NOT_RUN分别记录。旧25/27等历史结果不自动继承；最终以新构建完整相关测试为准。真人效率未测不写普通用户耗时或可用性已经通过。

---

## 21. 开发效率与最终交付

### 21.1 低token执行规则

- 开始只查工作区/分支状态、当前构建、相关入口/接口/算法/测试和参考组件；按I任务定向读取，不反复全库扫描。
- 保留一份短STATE和一份“需求→模块→测试→证据”矩阵，更新现有文档，不为每个补充新造平行说明体系。
- 接入与显示共用解析结果；科学任务和图层引用同一原件；共享派生物和缓存，不重复复制/读取全文件。
- 确定性解析、单位换算、模板绑定、渲染、统计和固定模型运行不新增LLM调用。确需LLM辅助提取/规划时按授权、预算和候选状态处理，不能代替科学判断。
- 先写能复现缺陷或需求的测试再实现，单项验证后整套回归；不靠删测试、删方法或放宽校验节省消耗。
- 无明确理由不换库、不重写已正确算法、不搭微服务集群；“无旧版兼容”减少技术债，不意味着无目的重复造轮子。
- 环境/会话中断，保存已完成/未验证/失败项与下一条具体动作，续接继续，不回到另一轮总体规划。

### 21.2 最终交付清单

1. 可运行代码和构建；新结构初始化、配置样例、依赖/镜像锁定和一条清楚的启动路径。
2. 数据接入工具与原件保护；自动图层、点查、显示/科学状态及大文件处理。
3. 全部I任务实际功能，A01—A52及继承目标的验收矩阵、真实证据和限制。
4. 从用户视角的操作说明：“选什么、自动做什么、什么时候才补充、失败怎么继续、成果在哪里”。
5. 标准/版本逐阶段能力矩阵、算法输入输出/数值基准、图件与标准成果样例（明确测试/真实身份）。
6. 新环境访问入口、启动/停止/诊断和备份恢复；正式替换方案与单独确认点。

最终不得只报告“已完成设计更新”“页面能打开”“预览成功”或“测试均通过”而无范围证据。不删除原始资料，不擅自清库/公开发布。新系统可以重组内部结构，但必须完整满足新的真实业务目标。

---

## 22. 可直接发给 Codex 的执行入口

```text
以 docs/coastmas-complete-development-design-20260924.md 作为本轮完整开发和验收基线。
它已合并最新综合评价流程、可视分析、专业工作台、模型上传和无旧版兼容要求。

最先落地：具有可靠定位的数据上传后自动建立图层、出图、定位并可原生点查。
查看资料不要求人工识别/描绘边界、创建实体/场景、填写科学定义或选择模型。
文件extent/有效mask自动读取；AOI/行政统计边界只在实际分析需要时引用或选择。
不允许把足迹生成做成另一个必须人工启动的识别任务。

按I01—I11内部依赖连续实施，覆盖全部业务与A01—A52，不只做地图Demo。
逐环节同步交付算法、专业界面、图/表联动和真实产物；先定向查当前实现，复用正确模块。
不要再做全站泛化审计、反复总体规划、旧API兼容层或另一套卡片长表单。

保留未知科学信息、正确网格/单位/时期、总量守恒、真实赋权和全域数值验收。
标准适配既要成功正向链路，也要不兼容反例；工程没实现不能写成等待用户。
模型包上传不自动执行，按隔离验证与版本资格发布。

服务端草稿/权限/版本/目录与运行恢复一起交付；预览、分析、样本、全域、业务验证分开。
一份短STATE和验收矩阵，定向读代码，减少无必要LLM和重复处理，不以漏功能/少测试省token。

允许按新设计修改代码和在隔离新环境初始化/验证；原始资料、现有未提交工作受保护。
正式环境替换、原数据删除、付费或公开发布仍需单独确认。
最终交付实际代码、可运行入口、真实标准数据与图件、逐项证据和未完成/阻塞状态。
```

---

## 23. 来源及设计依据

### 内部依据（已读取，对其有效要求作整合）

- D1：`coastmas-business-workbench-correction-20260924.md`，业务导航、目录/记录、方法与模型角色、原件/权限保护。
- D2：`coastmas-assessment-business-workflow-design-20260924.md`，用户真实综合评价流程、统计空间化、指标/权重/全域与验收。
- D3：`coastmas-visual-analysis-and-cartography-spec-20260924.md`，V01—V08、VV01—VV16及显示与分析分离。
- D4：本次截图与用户最新要求：已定位数据上传后自动显示，无人工边界前置。

本文对D1—D3中的“需要AOI/实体”的适用范围作了明确收紧：仅适用于确需该对象的科学分析，不适用于普通资料接入和显示。来源事实、算法假设和正式验证要求未因此放宽。

### 官方资料（2026-09-24核查；仅支撑技术机制，不证明CoastMAS已实现）

- S1 GDAL `gdalinfo`，格式、CRS、尺寸、变换/波段等读取： https://gdal.org/en/stable/programs/gdalinfo.html
- S2 GDAL geotransform与raster data model，栅格定位关系： https://gdal.org/en/stable/tutorials/geotransforms_tut.html ； https://gdal.org/en/stable/user/raster_data_model.html
- S3 GDAL `gdal_footprint`，根据nodata/mask生成有效覆盖： https://gdal.org/en/stable/programs/gdal_footprint.html
- S4 GDAL COG driver与OGC COG标准，显示优化与符合性/读取机制： https://gdal.org/en/stable/drivers/raster/cog.html ； https://docs.ogc.org/is/21-026/21-026.html
- S5 MapLibre地图与自动范围定位： https://www.maplibre.org/maplibre-gl-js/docs/API/classes/Map/ ； https://www.maplibre.org/maplibre-gl-js/docs/examples/fit-a-map-to-a-bounding-box/
- S6 OWASP File Upload Cheat Sheet，上传与隔离安全： https://cheatsheetseries.owasp.org/cheatsheets/File_Upload_Cheat_Sheet.html
- S7 W3C CSVW表格元数据规范： https://www.w3.org/TR/tabular-metadata/
- S8 CF Conventions，科学多维数据语义/版本： https://cfconventions.org/
- S9 OGC API—Processes，处理服务互操作： https://www.ogc.org/standards/ogcapi-processes/
- S10 WorldPop methods，统计与辅助资料空间分配方法： https://www.worldpop.org/methods/
- S11 W3C WCAG Use of Color，不仅依靠颜色传递信息： https://www.w3.org/WAI/WCAG22/Understanding/use-of-color.html
- S12 QGIS Print Layout，版式元素、模板/图集与输出参考： https://docs.qgis.org/3.44/en/docs/user_manual/print_layout/overview_layout.html

源资料中的研究方法及其适用边界继续保留。本文没有将这些标准、公开方法或截图转换成“系统已通过认证/新功能已PASS”的声明。具体规范子集、代码实现和实际表现以新版本证据为准。
