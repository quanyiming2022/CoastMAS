> **执行范围更新（2026-09-24）**：用户批准立即全新实现。本文有效业务/科学目标继续覆盖；旧兼容、固定页面/文件落点、增量改造与迁移条款均被取消，不再作为约束。“PROPOSED/仅设计/停止”指旧交付状态，不能阻止当前开发。新代码在`next/`，关键执行状态与验收映射见`next/STATE.md`、`next/acceptance.json`；不另写V3。旧环境、资料和证据保留，不建旧客户端兼容层。

> **当前架构落实**：任务草稿、受管原件与范围化依据统一服务端保存；方法/模型执行共用冻结清单与worker，标准服务作为受鉴权的薄适配。数据目录、配置成果、实体表/地图和运行历史围绕同一任务衔接。原模型与科学内核按新契约复用；不恢复旧兼容。实际验证及未实现项仅以`next/acceptance.json`为准。

> **成果与协同落实**：比较采用同项目不可变运行引用，复用来源与科学定义并固定成果哈希；讨论草稿按账号保存，技术审核单独记账并绑定运行，不修改科学有效性或自动作政策决定。B11/B12相关代码位于`next/`；剩余业务范围继续保留。

# 下一轮可执行任务与验收

**V2业务目标继续实施，尚未全范围验收。** 执行授权以上方最新范围为准；旧审计停止要求和旧版兼容条款已取消。不重新全库审计；定向实现并验证尚未完成能力。

范围依据：[report.md](report.md)、[inventory.json](inventory.json)、[redesign.md](redesign.md)。缺失独立范围文件仍须核对，不能把它不存在的条款当已满足。

## 1. 执行顺序与统一定义

执行依赖按最小可交付子项拆开，禁止B02与B05相互等待整个任务完成；B01—B13范围保留，默认一名执行者。

| 阶段 | 子项与前置 | 交付门槛 |
|---|---|---|
| 基础 | B01 → B05a（最小档案身份，无B02依赖）→ B04a（最小模板/绑定引用） | 统一版本身份和可重复验证 |
| 第一条闭环 | B09a依赖B04a；B02依赖B01/B05a/B04a/B09a；B03从B01开始 | 自动接入、最小复用与服务端草稿一起可用 |
| 扩展 | B04b、B05b在B02后，分别复用自身最小契约 | 扩展词表/方法及格式档案，无循环等待 |
| 全部下游 | B06依赖B04b/B05b/B03/B09a；B07依赖B06；B09b扩到全部编辑页 | 无重复录入且能持久恢复 |
| 模型与全域 | B10从B04a开始；B08依赖B06/B10 | 状态明确；算法级全域规则经验证 |
| 成果/全站 | B11 → B12；B13在B05b后补标准服务薄适配层 | 历史、权限、成果与所有入口保留 |
| 最终 | B01-final在B12/B13后 | 同一构建下完整相关回归，不以定向通过替代 |

完整无环依赖表保存在inventory.implementation_dependencies；a/b为原任务内部切片，不是删功能或要求用户阶段批准。

每项完成须有代码/契约diff、旧行为兼容测试、实际请求值与持久化/产物证据、适用角色与失败场景，相关lint/类型/构建；涉及UI必须真实浏览器。测试替身通过不能替代实际文件、真实执行器或科学基准。测试总套件失败必须保留，不能用挑选成功用例宣布整体PASS。

科学待确认与工程未实现分开：缺指标定义不阻止解析器/字段映射/大文件/草稿研发；适配器没写完、依赖没装好、审批交互缺失不能列“等用户”。不得用删除功能、放宽预检、屏蔽错误或无损标记覆盖有损转换。

## 2. 任务卡

### B01｜先稳定审计/全套验证与输入成本计量

- 对应：P16/P24，D09；依赖：无。
- 落点：`apps/web/e2e/projection-workflow.spec.ts`、`ui-consistency.spec.ts`、`scripts/e2e.py`及现有隔离夹具；复用本轮audit-fixture思想，避免把一次性本机绝对路径写入产品测试。
- 实现：各测试只依赖自己创建/明确继承的资源；大目录使用搜索/分页定位，不假定首20条；已有UI请求基线随夹具准备，缺失报清楚原因；不改基线掩盖提交语义差异。
- 验收：一次完整套件同一构建全部运行；单独和整套顺序均通过。至少41资产、多项目、多历史版本、超过一页账号/结果；两原失败可复现并说明修因。角色覆盖依据现有权限矩阵增加明确拒绝/允许断言。
- 计量：选择一套固定从零任务夹具，将独立事实、搜索、填写、确认、重复输入、导航、拖拽、恢复分别计数；API预置与用户步骤分开。人工用时须经真实使用测试，未测写NOT_RUN。
- 不做：删除原失败用例、缩成小目录、无审阅更新视觉快照。

### B02｜自动/批量接入与重复资源复用

- 对应：P01/P13，D01；依赖：B01、B05a、B04a、B09a；不等待B05b全部档案。
- 落点：`DataImport`/`DataWorkspace`、`ingestion_routes`、`file_ingestion`/`data_inspection`、对象存储；新增版本化接入会话/文件项最小契约，保留旧ingest、ingest-local及声明上传客户端。
- 实现：真实字节检测→原文件存储→技术事实→可复用声明→待补问题；批次共用来源/许可按适用集合绑定。支持现有支持格式的普通自动入口，不能只改按钮accept。重复哈希提供复用或独立声明资产选择；写幂等与失败恢复。
- 验收：40真实TIFF全部字节/hash对照；至少一个418826745字节文件浏览器实际上传、预览、完整下载；并测CSV、GeoJSON、GeoPackage、Shapefile、NetCDF、内部JSON格式。
- 反例：扩展名伪装、损坏容器、符号链接/路径穿越、越权来源、超限、半批失败、连接断开重试、同哈希不同许可、跨项目信息泄露。可登记未映射，不可冒充可计算。
- 迁移：旧资产不合并/重编号；重复现存41条仅给关联建议，禁止自动删一条。

### B03｜普通领域控件与内部ID选择

- 对应：P02/P14，D02；依赖：B01；B02/B04可逐项接入。
- 落点：`DataMetadataForm`、`ScientificFields`、`ModelEditor`、`DataSources`、`WorkflowEditor`、`Admin`、`Collaboration`，复用现有共享UI。
- 实现：按inventory全部UIF与契约字段清单交付“保留/自动/复用/专家/移出普通入口”的处理结果；普通数据用户不编辑JSON类型，管理员账号和方案结果用检索选择；模型维护者专家字段仍完整。
- 验收：旧入口展示改造核对原请求字段和值；新增引用入口允许请求结构不同，但服务端解析后的科学值、约束、来源、持久化契约与实际结果必须正确。条件字段、动态数组增删、nullable、0/false/空与未提供的差别、disabled/readOnly、键盘顺序均测试。
- 兼容：不删字段/参数、不改事件和科学默认值；类型生成同步，旧JSON/YAML仍可读；安全凭据不得纳入复用。
- 视觉：相关全站页面覆盖，三视口真实截图与键盘/标签/错误提示；地图/画布/弹层不得受公共样式污染。

### B04｜声明、指标语义、方法与任务适用的版本复用

- 对应：P03/P06/P17/P23，D03；依赖：B04a先依赖B05a；B04b再依赖B02/B04a，不反向阻塞B02。
- 落点：VariableSpec/BindingPlan、quality/lineage、validation、binding、planner与现有指标体系；增加Declaration/SemanticBinding/MethodProfile引用及失效判定。
- 实现：值带来源、状态、责任者、作用范围/版本；文件事实缓存、认可模板、本次绑定分层复用；新文件结构和科学适用条件仍匹配时沿用模板，新建绑定并重验；明确科学缺口列表；单位/别名确定适配自动，有损或改变解释的集中确认。
- 验收：同一数据在场景/时间/评价/优化/模型准备间不重填单位、许可、已知时期；源/方法/词表/权限/范围变化只使相关信息失效。2022年不生成全年日期；未知方向不猜；原业务A/B/C不混作实验A/B/C。
- 反例：m→km精确转换、温标offset、量纲不兼容、总量/强度不同、垂向基准不同、类别近似语义、已过期许可、跨项目误复用。LLM候选不能自动批准映射。
- 兼容：旧显式值保留原版；新引用可选，冲突必须报错；不篡改旧manifest/结果。
- 科学BLOCKED仅限：无认可字典/方法时不得产正式评价；工程模板使用独立、标记清楚的测试夹具继续实现。

### B05｜分阶段标准/版本能力与可追溯适配

- 对应：P04/P05/P19/P22，D04；依赖：B05a只依赖B01，交付最小版本身份/阶段能力契约；B05b依赖B02/B05a，扩展格式与反例，不等待B04b全量完成。
- 落点：既有解析器、连接器、适配内核、validation；一份类型化profile registry、AdaptationPlan与导出能力查询。
- 实现：标准/版本/扩展自动检测；普通入口只在歧义时选择。每档案分别声明识别、结构、语义、映射、任务、导出与损失，不设一个“支持”布尔值冒充全部阶段。
- 验收格式：COG布局及Range行为分开；GeoPackage版本和多图层；GeoJSON单体/集合/三维边界；ZIP字段/编码；CSV分号/引号/BOM/前导零/空值；JSON领域schema已知/未知。
- CF验收：保留calendar/time bounds/cell_methods/coordinate/grid_mapping/packing；CF1.12与选定1.13能力分别有证据。360_day不能静默当UTC/Gregorian；读取、时间适配和导出往返逐阶段核对。未支持组合只阻断相关运算，不丢原文。
- STAC/CSVW：从标准文档构造真实规范夹具，验证资产关系/列类型/缺值与扩展版本；受控外部源明确授权/认证边界。外部服务不可达时可用本地规范服务检验协议，但真实端点验收单列NOT_RUN/BLOCKED。
- 损失验收：有损输出与转换展示具体丢失字段/精度/维度/日历影响；确认记录绑定源与目标版本。无损round-trip比较语义及必要字节，不只比较能打开。
- 迁移：旧profile未知保留；不自动宣称符合最新标准。版本支持是明确子集，未测扩展不算PASS。

### B06｜资产到实体/时间/场景的共同映射

- 对应：P08/P09，D05；依赖：B02/B04/B05基础。
- 落点：`Entities`、`TemporalAdaptation`、`SceneWorkspace`、`geography_routes`、`adaptation_routes`；复用现有实体版本和时间内核。
- 实现：选择固定资产/图层/变量，通过ObservationMapping读取ID/几何/时间/值/单位；批量预览与逐项错误；场景继承真实覆盖而非猜测公共时期。
- 验收：FeatureCollection/GeoPackage多实体一次映射；CSV和CF时间序列不手录原值；单体手绘/手动观测继续能用。实体创建时间与有效期分开。
- 反例：重复ID×时期、缺几何、坐标异常、部分有效、缺测、非法日历、分组时间支撑不同、观察点与管理单元不一致。TN仅提供正确CRS证据后生成派生纠正版本，不改原文件。
- 兼容：旧显式请求仍接受；新mapping_ref经服务端解析成同一内核契约，不能信任前端预览覆盖服务端校验。

### B07｜资产到评价/优化输入与真实业务模板

- 对应：P07/P17，D05；依赖：B04/B05/B06映射。
- 落点：`IndicatorFrameworks`、`indicator_routes`/`indicator_framework`、`SpatialOptimization`/`optimization_routes`。
- 实现：复用已实现栅格→评价单位转换；支持表格/实体资产源列映射；目标/权重/参考范围由认可模板；优化批量生成候选单元、成本、风险、面积及保护约束。
- 验收：原400行m→km链路继续误差在既有容差内、位置/行身份/样本范围不丢；大于一页候选输入和部分错误可定位；无需用户写indicator_frame JSON或重复键入已有数据。
- 科学反例：缺成本不能填0、缺权重不能静默等权、方向未知不能凭缩写、总量/强度约束不能绕过、protected单元不能默认允许。输入顺序变更不能错配实体。
- 工程与业务分验：工程用显式测试模板通过；正式业务需认可目标/模板与真实独立验证，不伪造分区或最优决策。

### B08｜模型任务导向准备及真实全图推理

- 对应：P10，D06；依赖：B04/B05/B06、B10运行资格。
- 落点：`RasterFrame`、`raster_frame`、projection适配器、现有worker/结果写出。
- 实现：显式选模型任务决定特征/响应槽；保留当前样本链路；全图按所选算法定义训练/推理阶段、固定标准化参数、分块I/O和恢复点。
- 先决设计：PPCI的训练与全域赋类规则、pprRFA训练/预测接口和验证分割必须明确；不能每块独立重训拼接后声称数值等价。
- 验收：原包小样本数值对照；独立小全图基准与窗口输出逐像元/掩膜/坐标对照；真实11416×9000输入端到端资源预算、共同有效分母、取消恢复、完整产物哈希。性能/RSS如实测量，不只修改上限。
- 输出标明训练范围、样本/全域、方法参数/版本、未覆盖区。缺业务响应仅阻断正式回归结论，运行架构研发照常。

### B09｜任务草稿、返回与失败恢复

- 对应：P11/P21，D07；依赖：B09a依赖B04a并与第一条少填链路交付；B09b依赖B03/B04b/B09a扩展到全站。
- 落点：现有workspace上下文、TemporalAdaptation及其他编辑入口、API写入幂等；不另建任务状态机替代Job。
- 实现：项目内服务端持久草稿是必选能力，保留必要编辑内容、引用与步骤，覆盖跨页/刷新/关闭后重开；恢复重验源版本/权限；断网/校验失败只修有关字段；有必要才离开提醒，密码/密钥/大文件不落本机持久存储。
- 验收：本轮“填名称→离开→后退”变为原值恢复；扩展到场景/模型/工作流/评价/优化/协同。刷新、浏览器前后退、会话过期、切项目、源变更、并发冲突、关闭重开分别实测；成功保存过的有效内容不得因仅本机状态丢失而重填。离线未同步状态需明确显示，恢复网络后幂等合并且不覆盖他人版本。
- 反例：重试不重复建资产/作业，旧权限失效不能读出草稿，恢复旧值不能覆盖他人新版本。后台轮询、地图和运行不中断。

### B10｜模型及本地报告的统一状态解释

- 对应：P12/P15，D08；依赖：B04a身份/版本，不等待全部下游实现。
- 落点：`BusinessIntake`、`ProvidedModels`、模型目录、`projection_routes`与现有受信release。
- 实现：源码发现、技术验证、项目审批、当前输入适用、工程执行、业务有效分别显示；报告离线状态加检查时间及边界，关联项目后按源hash查实际状态。
- 验收：本轮本地报告NOT_EXECUTABLE与实际v2审批并存时不误导；相同名字不同源码不能自动关联；release缺失/失配/审批版本变化有真实错误与可恢复入口。
- 权限：保持项目admin审批与系统管理员账号管理的区别；VIEWER/PUBLIC不能注册/批准，具体MANAGER/RESEARCHER权限按原矩阵，前端隐藏不代替后端校验。
- 技术：继续运行两包原实现数值对照、真实PRD两流程；iris/工程响应不计正式业务验证。

### B11｜真实成果表达、可比性与标准导出

- 对应：P18，D10；依赖：B04/B05/B07/B08相应能力。
- 落点：`Results`、`ProjectionResultView`、`AssessmentResults`、`GeoMap`、现有下载/结果API。
- 实现：真实值驱动专题地图/图例、表格与选择；统一显示源版本/方法/范围/损失；导出profile提供地图、表格、标准文件及来源说明，不只裸JSON。
- 验收：cluster图例来自实际簇号不含虚构风险等级；回归主题/残差与400行结果逐行匹配；分页/下载全量数量一致，样本与全图清楚区分。
- 可比性：单位、时期、方法、标准化基准不同需解释或禁比；导出有损先提示实际损失，并保留原产物。历史不可变结果仍可下载/重现。
- 浏览器：网络底图真实可用性单独测；离线时数据层可用且说明缺底图，不能用空地图冒称网络验证PASS。

### B12｜全站任务连贯性、历史和文档

- 对应：P20及用户全站最少往返要求，D11；依赖：B02–B11各页面上线后逐步覆盖。
- 落点：现有Sidebar/navigation/workspace、目录/选择器、Admin与详细使用手册。
- 实现：任务上下文带到下一步，保留旧路由；历史/工程/合成/正式状态可辨，默认入口适合真实工作。已有可读结果、场景、模型可直接复用，禁止复制第二项目选择器。
- 验收：inventory全部现有入口逐项无遗漏/重复/404；详情/版本/编辑直达、query/hash/返回路径、角色加载、长目录/邮箱、移动抽屉和无有效项目。全站表格空/筛选空/加载/失败分别可识别。
- 历史：归档仅走现有授权操作，依赖保留和可恢复；旧合成项目不擅自删除。批量范围明确，不把当前页操作冒称全部页。
- 文档：按最终真实行为更新已有详细手册和UI指南，不再新建平行文档体系；每功能含“选什么、自动什么、何时补充、失败怎么恢复、产物去哪”。

### B13｜标准任务服务互操作（边界明确的适配层）

- 对应：标准矩阵S10及用户多方法/标准版本要求，D04；依赖：B05/现有Job与manifest稳定。
- 落点：现有run_routes/模型描述/结果存储前的薄适配层，不改现有状态机和权限。
- 实现：先给出OGC API Processes Core 1.0适用性和支持子集，再接process description、输入输出格式/异步job映射；对外接口不默认为公网发布。普通用户选方法，由系统决定协议。
- 验收：标准发现、请求/响应schema、真实执行/失败/取消与成果链接；同一CoastMAS任务经自有API和适配层数值/来源一致。未知扩展/version返回明确不支持，不能声称完整认证。
- 本项不替代内部业务闭环；若后续范围裁定无需对外服务，应记录用户决定而非在本轮擅自删除。

## 3. 契约和历史迁移统一门禁

| 拟契约 | 旧行为保留 | 必测 |
|---|---|---|
| IngestionSession/文件项 | 原ingest/声明上传可读写 | 同源重试、部分失败、原字节、旧客户端 |
| Declaration/SemanticBinding/MethodProfile | 旧内嵌值与版本保留 | 不同精度/未知值、作用域失效、许可/权限 |
| FormatProfile/AdaptationPlan | 旧格式路径和原文件可访问 | 每版本每阶段、无损/有损、未知扩展 |
| ObservationMapping及source_refs | 旧显式领域输入继续接受 | 值冲突拒绝、行身份/单位/日历、分母 |
| TaskDraft | 原Job/资源状态机不变 | 跨项目与过期权限、恢复/并发、敏感数据 |
| 成果导出档案引用 | 旧manifest/result不可变 | 历史可读、hash及语义、可比性和损失 |

新增schema版本需生成前后端类型，服务端复验所有客户端推导结果。迁移采取加字段/新关联和按需显式确认，不批量将未知当已知；旧数据解释仍按旧版本，可重算但生成新结果。

## 4. V2专项回归（纳入原任务，不增加阶段确认）

- 字段语义：比较复选框保持用户选择和跨页状态；资源选择不因“版本”被隐藏；种子从方法/运行模板冻结；方法依据不从影像空间范围自动生成；发布输出不因可读取名称就自动选中。
- 新批次：不同hash但结构和适用范围相符时，重新读事实、新建绑定、复用认可语义；科学定义重复填写0次。相同文件但方法/权限/许可变更，只有相关复用失效。
- 首次未知资料：自动列变量映射表，负责人按适用组补一次；不得一键猜单位、方向或类别，也不得逐文件重复问。
- 任务入口：从资产、模型、场景、评价/优化、旧结果发起后，已选择的项目/资源/方法引用只传递不重选；不复制新项目选择器。
- 一键预检并运行：当前配置冻结、依赖重验与Job幂等提交都发生；六类状态独立，输入变化后旧预检不能重用。
- 持久恢复：返回、刷新、关闭重开、断网、引用变化与权限失效均测；不存密码/凭据/字节矩阵，只补真正变化的信息。
- 完整相关浏览器测试仍须同构建整套执行；25/27为原审计首轮事实，本次文档修订没有产生新的产品测试PASS。

## 5. 下一轮最终验收记录模板

每条B任务及每种实际格式/模型至少记录：范围/输入真实hash、代码与标准/模板版本、角色、操作数及独立事实数、请求字段和值核对、持久化对象与版本、运行ID/真实执行器、输出hash/行数/单位/范围、数值基准与容差、错误/恢复、截图及命令退出码。

分别填写工程PASS/FAIL、业务PASS/FAIL/BLOCKED、未运行NOT_RUN；缺科学依据不能填PASS，部分格式通过不能替整套标准族通过。最低完整链路：受管原文件→可复用映射→任务预检→实际worker→真实地图/表格/全量下载。界面能打开、HTTP200、演示数据或旧证据均不能替代。

本轮仅交付这些任务卡，不执行上述改造。
